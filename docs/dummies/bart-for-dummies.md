# bart for Dummies

This book is maintained by DumDum. It explains files after they have stopped changing, focusing on what each part means for users and future maintainers.

## Contents

- [`.kaptaind/analysis/186039ca-ab0b-467d-a4b5-ac2a0bef1b40.json`](#kaptaindanalysis186039ca-ab0b-467d-a4b5-ac2a0bef1b40json)
- [`.kaptaind/analysis/18bf3896-9dc5-4e1d-af12-30a9f7ad228c.json`](#kaptaindanalysis18bf3896-9dc5-4e1d-af12-30a9f7ad228cjson)
- [`.kaptaind/analysis/1ad62013-5ec1-45a6-9603-4bad9cd5c410.json`](#kaptaindanalysis1ad62013-5ec1-45a6-9603-4bad9cd5c410json)
- [`.kaptaind/analysis/1f73563d-99a6-4718-ac9b-0559f6ecc9c1.json`](#kaptaindanalysis1f73563d-99a6-4718-ac9b-0559f6ecc9c1json)
- [`.kaptaind/analysis/1f84a3bd-0de6-42a9-801d-b476906377f5.json`](#kaptaindanalysis1f84a3bd-0de6-42a9-801d-b476906377f5json)
- [`.kaptaind/analysis/1ffd7c42-112f-4c70-9aba-bdd24b3c1488.json`](#kaptaindanalysis1ffd7c42-112f-4c70-9aba-bdd24b3c1488json)
- [`.kaptaind/analysis/2d0d6362-87c2-4db1-84af-e03487fea83c.json`](#kaptaindanalysis2d0d6362-87c2-4db1-84af-e03487fea83cjson)
- [`.kaptaind/analysis/3064b1e7-a537-4d35-98c0-338fe17a5af3.json`](#kaptaindanalysis3064b1e7-a537-4d35-98c0-338fe17a5af3json)
- [`.kaptaind/analysis/3081d55f-c455-48bc-b11e-00618089bf7d.json`](#kaptaindanalysis3081d55f-c455-48bc-b11e-00618089bf7djson)
- [`.kaptaind/analysis/316c123b-ca9a-4ee3-a62f-2f05b97b6e82.json`](#kaptaindanalysis316c123b-ca9a-4ee3-a62f-2f05b97b6e82json)
- [`.kaptaind/analysis/35d9392a-93bb-4536-8877-80b0f13990dc.json`](#kaptaindanalysis35d9392a-93bb-4536-8877-80b0f13990dcjson)
- [`.kaptaind/analysis/39e0e23d-c16d-400d-8c67-5c4e4e22e847.json`](#kaptaindanalysis39e0e23d-c16d-400d-8c67-5c4e4e22e847json)
- [`.kaptaind/analysis/3c909eac-ce73-4996-bb81-d11627e78211.json`](#kaptaindanalysis3c909eac-ce73-4996-bb81-d11627e78211json)
- [`.kaptaind/analysis/3dd6adad-7e48-4eb9-ad34-40e8df51c8d6.json`](#kaptaindanalysis3dd6adad-7e48-4eb9-ad34-40e8df51c8d6json)
- [`.kaptaind/analysis/41931d6b-d6f4-4141-87a3-2d135b6f8d31.json`](#kaptaindanalysis41931d6b-d6f4-4141-87a3-2d135b6f8d31json)
- [`.kaptaind/analysis/43fed0a2-1fbc-4d09-a99c-10df75a102e9.json`](#kaptaindanalysis43fed0a2-1fbc-4d09-a99c-10df75a102e9json)
- [`.kaptaind/analysis/4652ed92-bccd-4b3a-978f-289c0909e89f.json`](#kaptaindanalysis4652ed92-bccd-4b3a-978f-289c0909e89fjson)
- [`.kaptaind/analysis/56169df8-9234-4b85-b891-f017005852eb.json`](#kaptaindanalysis56169df8-9234-4b85-b891-f017005852ebjson)
- [`.kaptaind/analysis/5d7888f9-80eb-4726-a4c5-5a944754cc64.json`](#kaptaindanalysis5d7888f9-80eb-4726-a4c5-5a944754cc64json)
- [`.kaptaind/analysis/60a479c2-bcc1-4506-a70e-6f07dcdf8acf.json`](#kaptaindanalysis60a479c2-bcc1-4506-a70e-6f07dcdf8acfjson)
- [`.kaptaind/analysis/64e914c1-6703-4b74-9dee-c12b367415a2.json`](#kaptaindanalysis64e914c1-6703-4b74-9dee-c12b367415a2json)
- [`.kaptaind/analysis/6858827f-a62c-43db-b55d-a8546198592c.json`](#kaptaindanalysis6858827f-a62c-43db-b55d-a8546198592cjson)
- [`.kaptaind/analysis/79a23395-c066-4c97-839e-62bc345ec57b.json`](#kaptaindanalysis79a23395-c066-4c97-839e-62bc345ec57bjson)
- [`.kaptaind/analysis/7cbb8411-2693-46fb-8f7c-65bc4bed838c.json`](#kaptaindanalysis7cbb8411-2693-46fb-8f7c-65bc4bed838cjson)
- [`.kaptaind/analysis/7e4a0d07-d565-4377-80e1-0cba006ec3ba.json`](#kaptaindanalysis7e4a0d07-d565-4377-80e1-0cba006ec3bajson)
- [`.kaptaind/analysis/7e6099f4-de05-47b7-a18e-36b1ae02d836.json`](#kaptaindanalysis7e6099f4-de05-47b7-a18e-36b1ae02d836json)
- [`.kaptaind/analysis/80189b65-d7b2-45e1-b7e0-9aaad0582dfd.json`](#kaptaindanalysis80189b65-d7b2-45e1-b7e0-9aaad0582dfdjson)
- [`.kaptaind/analysis/826ac6d3-3e28-49b2-b988-5d2b1a53b25e.json`](#kaptaindanalysis826ac6d3-3e28-49b2-b988-5d2b1a53b25ejson)
- [`.kaptaind/analysis/82c2ec14-13b8-4dd7-a9d0-fb6dc8a174f8.json`](#kaptaindanalysis82c2ec14-13b8-4dd7-a9d0-fb6dc8a174f8json)
- [`.kaptaind/analysis/85ae96da-8f9a-4095-99c4-6e40214da2e7.json`](#kaptaindanalysis85ae96da-8f9a-4095-99c4-6e40214da2e7json)
- [`.kaptaind/analysis/85d6176e-d67e-4e36-9451-4c35813bbc95.json`](#kaptaindanalysis85d6176e-d67e-4e36-9451-4c35813bbc95json)
- [`.kaptaind/analysis/89cf3209-9bfc-47e9-b2a1-a47cfb425c4a.json`](#kaptaindanalysis89cf3209-9bfc-47e9-b2a1-a47cfb425c4ajson)
- [`.kaptaind/analysis/9276a9fb-4515-4314-8c03-7dcbb1412f66.json`](#kaptaindanalysis9276a9fb-4515-4314-8c03-7dcbb1412f66json)
- [`.kaptaind/analysis/9caa3d87-e537-48fa-937d-a7b643fe055f.json`](#kaptaindanalysis9caa3d87-e537-48fa-937d-a7b643fe055fjson)
- [`.kaptaind/analysis/9d690c3f-5d82-4fec-93ca-cbb31bb61820.json`](#kaptaindanalysis9d690c3f-5d82-4fec-93ca-cbb31bb61820json)
- [`.kaptaind/analysis/a099ca29-af06-4095-9a0e-e97e54e01072.json`](#kaptaindanalysisa099ca29-af06-4095-9a0e-e97e54e01072json)
- [`.kaptaind/analysis/a4ed4b1a-a3c9-473c-b9aa-663b5b424a43.json`](#kaptaindanalysisa4ed4b1a-a3c9-473c-b9aa-663b5b424a43json)
- [`.kaptaind/analysis/a6185f86-99a8-4c23-a13d-15576dd9f361.json`](#kaptaindanalysisa6185f86-99a8-4c23-a13d-15576dd9f361json)
- [`.kaptaind/analysis/af2be5aa-bbf4-4df2-afba-c28743a73d15.json`](#kaptaindanalysisaf2be5aa-bbf4-4df2-afba-c28743a73d15json)
- [`.kaptaind/analysis/b11eba79-6595-4939-91d4-cc76b5730986.json`](#kaptaindanalysisb11eba79-6595-4939-91d4-cc76b5730986json)
- [`.kaptaind/analysis/b1ff7acd-af35-4e58-b4f3-cf0c06584e31.json`](#kaptaindanalysisb1ff7acd-af35-4e58-b4f3-cf0c06584e31json)
- [`.kaptaind/analysis/b39b69cf-6a51-4371-8b5f-63490e0d59ff.json`](#kaptaindanalysisb39b69cf-6a51-4371-8b5f-63490e0d59ffjson)
- [`.kaptaind/analysis/b5c3f66c-b849-4b98-b408-ac938935697d.json`](#kaptaindanalysisb5c3f66c-b849-4b98-b408-ac938935697djson)
- [`.kaptaind/analysis/b5c436cd-923c-4fed-b49c-9c5db2140f94.json`](#kaptaindanalysisb5c436cd-923c-4fed-b49c-9c5db2140f94json)
- [`.kaptaind/analysis/b744be03-d46e-4164-8868-5df42c58957a.json`](#kaptaindanalysisb744be03-d46e-4164-8868-5df42c58957ajson)
- [`.kaptaind/analysis/c82668b4-4835-4252-9175-b5f4c44bb8a0.json`](#kaptaindanalysisc82668b4-4835-4252-9175-b5f4c44bb8a0json)
- [`.kaptaind/analysis/c90ba5bd-5656-4ae7-8032-406520e01f9c.json`](#kaptaindanalysisc90ba5bd-5656-4ae7-8032-406520e01f9cjson)
- [`.kaptaind/analysis/ca7e3a2a-62e0-4725-93a5-184d475b8eaf.json`](#kaptaindanalysisca7e3a2a-62e0-4725-93a5-184d475b8eafjson)
- [`.kaptaind/analysis/ce9f36f4-92d2-489a-9405-cfe9edf84b28.json`](#kaptaindanalysisce9f36f4-92d2-489a-9405-cfe9edf84b28json)
- [`.kaptaind/analysis/d36405a2-13ff-41a4-a544-0f10c4b623d1.json`](#kaptaindanalysisd36405a2-13ff-41a4-a544-0f10c4b623d1json)
- [`.kaptaind/analysis/d526aea9-fbeb-401a-84de-93dea41863dc.json`](#kaptaindanalysisd526aea9-fbeb-401a-84de-93dea41863dcjson)
- [`.kaptaind/analysis/d9dc6bf3-f172-4c0d-8047-344b102bd368.json`](#kaptaindanalysisd9dc6bf3-f172-4c0d-8047-344b102bd368json)
- [`.kaptaind/analysis/de5260f4-caa3-4a94-8158-649d8eb9fcbd.json`](#kaptaindanalysisde5260f4-caa3-4a94-8158-649d8eb9fcbdjson)
- [`.kaptaind/analysis/e1d79b62-5210-4e80-8361-f53193a9c23a.json`](#kaptaindanalysise1d79b62-5210-4e80-8361-f53193a9c23ajson)
- [`.kaptaind/analysis/e2654113-876b-4556-a7b6-91c232111371.json`](#kaptaindanalysise2654113-876b-4556-a7b6-91c232111371json)
- [`.kaptaind/analysis/e42b9c44-0232-4d4f-a3da-0ffcf233f1db.json`](#kaptaindanalysise42b9c44-0232-4d4f-a3da-0ffcf233f1dbjson)
- [`.kaptaind/analysis/e7f51231-94cb-4ade-af6b-1d830f086b69.json`](#kaptaindanalysise7f51231-94cb-4ade-af6b-1d830f086b69json)
- [`.kaptaind/analysis/e9d12405-54c9-4aba-bd4a-b1b2c230ddbe.json`](#kaptaindanalysise9d12405-54c9-4aba-bd4a-b1b2c230ddbejson)
- [`.kaptaind/analysis/ee10746a-c6ed-4ea0-8049-fb2e40240cb2.json`](#kaptaindanalysisee10746a-c6ed-4ea0-8049-fb2e40240cb2json)
- [`.kaptaind/analysis/ef65a75d-c6ab-4fa1-b21d-30f3a94e6749.json`](#kaptaindanalysisef65a75d-c6ab-4fa1-b21d-30f3a94e6749json)
- [`.kaptaind/analysis/faca43da-efc8-4d9e-a80d-4a2184b66a01.json`](#kaptaindanalysisfaca43da-efc8-4d9e-a80d-4a2184b66a01json)
- [`.kaptaind/analysis/fcebe2fb-9a7d-4c1b-9e00-2b6c2003cf72.json`](#kaptaindanalysisfcebe2fb-9a7d-4c1b-9e00-2b6c2003cf72json)
- [`.kaptaind/analysis/ff55fd57-3516-4cca-89a9-7da268899136.json`](#kaptaindanalysisff55fd57-3516-4cca-89a9-7da268899136json)
- [`.kaptaind/status.json`](#kaptaindstatusjson)
- [`.kaptaind/telemetry.json`](#kaptaindtelemetryjson)
- [`.kaptaind/version_cache.json`](#kaptaindversioncachejson)
- [`AGENTS.md`](#agentsmd)
- [`Cargo.toml`](#cargotoml)
- [`README.md`](#readmemd)
- [`assets/vhs/daemon_observability.tape`](#assetsvhsdaemonobservabilitytape)
- [`assets/vhs/differential.tape`](#assetsvhsdifferentialtape)
- [`assets/vhs/interactive_tui.tape`](#assetsvhsinteractivetuitape)
- [`assets/vhs/scan_core.tape`](#assetsvhsscancoretape)
- [`demo.tape`](#demotape)
- [`kaptaind.toml`](#kaptaindtoml)
- [`scripts/generate_demos.sh`](#scriptsgeneratedemossh)
- [`src/main.rs`](#srcmainrs)

<!-- DUMDUM:START 2573223082376390813 -->
## `.kaptaind/analysis/186039ca-ab0b-467d-a4b5-ac2a0bef1b40.json`

**File: `.kaptaind/analysis/186039ca-ab0b-467d-a4b5-ac2a0bef1b40.json`**

##### What it is and where it sits in the project

This file is a JSON file located in the `.kaptaind/analysis` directory. It contains analysis data for a specific cluster.

##### Why it matters to users or maintainers

This file provides information about the analysis of a cluster, including the version of the analysis, the type of bump (Patch), the number of events, and the time it took to complete the analysis. This information can be useful for understanding the performance and behavior of the analysis process.

##### User-visible behavior or operational effect

This file does not directly affect user-visible behavior, but it can be used to inform decisions about the analysis process, such as determining whether to re-run the analysis or to investigate issues with the analysis.

##### Maintainer notes and review checklist

* Review the file to ensure that it contains accurate and up-to-date information about the analysis.
* Verify that the file is properly formatted and that it conforms to the expected structure.
* Check that the file is not causing any issues with the analysis process or with other parts of the system.
* Consider adding additional metadata or analysis data to the file to provide more insight into the analysis process.

Note: The file appears to be a JSON object containing analysis data for a cluster. The data includes information about the version of the analysis, the type of bump, the number of events, and the time it took to complete the analysis. The file does not contain any references to GIFs or images.
<!-- DUMDUM:END 2573223082376390813 -->

<!-- DUMDUM:START 5343332601906894649 -->
## `.kaptaind/analysis/18bf3896-9dc5-4e1d-af12-30a9f7ad228c.json`

**.kaptaind/analysis/18bf3896-9dc5-4e1d-af12-30a9f7ad228c.json: A JSON File in the Bart Project**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the Bart project. It contains configuration data for tooling or runtime behavior.

**Why it matters to users or maintainers:**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not directly interact with this file, but its impact can still be significant.

**User-visible behavior or operational effect:**
The data in this file is likely used to inform decisions about the project's configuration, such as determining the version of the project, identifying changes, and assessing the impact of those changes.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**File content:**
```json
{
  "cluster_id": "18bf3896-9dc5-4e1d-af12-30a9f7ad228c",
  "version": "0.1.50",
  "bump": "Patch",
  "event_count": 87,
  "started_at": "2026-07-03T14:39:24.085155829Z",
  "ended_at": "2026-07-03T14:39:24.707294365Z",
  "diff": {
    "structural": 0.85466504,
    "api": 0.0,
    "deps": 0.5861111,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 25,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 2,
    "dependency_nodes": 14,
    "dependency_edges": 13,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.37362173,
    "api_breaking": false,
    "api_added": false
  },
  "air_gapped": false
}
```

This file appears to contain metadata about a project analysis, including version information, event counts, and diff data. The "diff" section provides details about changes to the project, including structural, API, and dependency-related metrics. The "weight" section seems to provide a score and flags related to API breaking and adding changes.
<!-- DUMDUM:END 5343332601906894649 -->

<!-- DUMDUM:START 18016449948320414231 -->
## `.kaptaind/analysis/1ad62013-5ec1-45a6-9603-4bad9cd5c410.json`

**.kaptaind/analysis/1ad62013-5ec1-45a6-9603-4bad9cd5c410.json File Explanation**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers:**
This file contains configuration data for the `kaptaind` tool, which is used to analyze and monitor the project. The data in this file affects the reliability, output, and workflow of the project.

**User-visible behavior or operational effect:**
The data in this file is used to calculate a "weight" score, which indicates the likelihood of a breaking change in the project. This score is used to inform the project's maintenance and development decisions.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**File content:**
The file contains a JSON object with the following properties:

* `cluster_id`: a unique identifier for the cluster
* `version`: the version of the `kaptaind` tool
* `bump`: the type of change (Patch, Minor, Major)
* `event_count`: the number of events processed
* `started_at` and `ended_at`: timestamps for the analysis period
* `diff`: an object containing metrics for the analysis period
* `weight`: an object containing a score and flags for breaking and added changes
* `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

Note: The file does not contain any references to GIFs, images, or VHS recordings.
<!-- DUMDUM:END 18016449948320414231 -->

<!-- DUMDUM:START 7036904839780271541 -->
## `.kaptaind/analysis/1f73563d-99a6-4718-ac9b-0559f6ecc9c1.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/1f73563d-99a6-4718-ac9b-0559f6ecc9c1.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains analysis data, which is likely used to inform decisions about the project's configuration or behavior. Although users may not directly interact with this file, its contents can affect the project's reliability, output, or workflow.

**User-visible behavior or operational effect.**
The file's contents are not directly visible to users, but they can influence the project's behavior in various ways. For example, the analysis data may be used to determine which dependencies to install or which configuration settings to apply.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains analysis data, including a cluster ID, version, bump type, event count, start and end times, and various metrics related to the analysis. The data suggests that the analysis was performed on a specific version of the project, and the results indicate a relatively low structural diff and no API breaking changes. The file also includes information about the weight of the analysis and whether it was air-gapped.
<!-- DUMDUM:END 7036904839780271541 -->

<!-- DUMDUM:START 6641394186870193312 -->
## `.kaptaind/analysis/1f84a3bd-0de6-42a9-801d-b476906377f5.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/1f84a3bd-0de6-42a9-801d-b476906377f5.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains metadata about the analysis of the project, including the version of the tool used, the type of event that occurred, and the duration of the analysis. This information can be useful for maintainers to understand the project's history and make informed decisions about future development.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior or operational effect. However, the analysis data contained within can inform decisions about future development and maintenance.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

**Additional notes:**
The file contains a JSON object with various properties, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` property contains additional metadata about the analysis, including structural, API, and dependency-related metrics. The `weight` property contains a score and API-related flags.
<!-- DUMDUM:END 6641394186870193312 -->

<!-- DUMDUM:START 16084225727518131068 -->
## `.kaptaind/analysis/1ffd7c42-112f-4c70-9aba-bdd24b3c1488.json`

**What it is and where it sits in the project.**
This is a JSON file named `1ffd7c42-112f-4c70-9aba-bdd24b3c1488.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens. Its behavior can still affect reliability, output, or workflow, even if users don't touch it directly.

**User-visible behavior or operational effect.**
The file contains metrics and statistics about the project's analysis, such as the number of events, started and ended times, and a diff object with various metrics like structural, API, and dependency changes.

**Maintainer notes and review checklist.**
Keep the generated explanation aligned when this file changes. Confirm the explanation still matches the file after major edits, check whether linked commands, images, GIFs, or VHS tapes still exist, and re-run DumDum after the file has rested so generated sections stay aligned.

Here's a breakdown of the file's content:

* `cluster_id`: a unique identifier for the analysis cluster
* `version`: the version of the tooling or runtime being used
* `bump`: the type of version bump (Patch, Minor, Major)
* `event_count`: the number of events processed
* `started_at` and `ended_at`: timestamps for when the analysis started and ended
* `diff`: an object containing various metrics and statistics about the analysis
* `weight`: an object containing a score and flags for API breaking and added changes
* `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

This file is likely used for monitoring and analytics purposes, providing insights into the project's analysis and tooling behavior.
<!-- DUMDUM:END 16084225727518131068 -->

<!-- DUMDUM:START 13619773211464217767 -->
## `.kaptaind/analysis/2d0d6362-87c2-4db1-84af-e03487fea83c.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/2d0d6362-87c2-4db1-84af-e03487fea83c.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains analysis data for the `bart` project, including information about the version, bump type, event count, and diff statistics. This data is likely used to inform decisions about the project's maintenance and development.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior or operational effects. However, the data it contains may influence decisions about the project's maintenance and development, which can indirectly impact user experience.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the analysis data is accurate and up-to-date. They should also check that the file is properly formatted and that any changes to the file do not break the project's build or functionality.

Here is a summary of the file's contents:

* `cluster_id`: a unique identifier for the analysis run
* `version`: the version of the `bart` project being analyzed
* `bump`: the type of bump (e.g. patch, minor, major)
* `event_count`: the number of events in the analysis run
* `started_at` and `ended_at`: timestamps for the start and end of the analysis run
* `diff`: a dictionary containing statistics about the diff between the analyzed versions
* `weight`: a dictionary containing a score and flags for API breaking and added changes
* `air_gapped`: a flag indicating whether the analysis was run in an air-gapped environment

Note that the file contains a large amount of data, and the above summary only highlights the most important fields.
<!-- DUMDUM:END 13619773211464217767 -->

<!-- DUMDUM:START 7993302248721001788 -->
## `.kaptaind/analysis/3064b1e7-a537-4d35-98c0-338fe17a5af3.json`

**File: `.kaptaind/analysis/3064b1e7-a537-4d35-98c0-338fe17a5af3.json`**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It contains configuration data for tooling or runtime behavior.

**Why it matters to users or maintainers:**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect:**
The file contains data about the project's analysis, including the cluster ID, version, bump type, event count, and timing information. It also includes a diff object with metrics about structural, API, and runtime changes.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Untrusted file content:**
```json
{
  "cluster_id": "3064b1e7-a537-4d35-98c0-338fe17a5af3",
  "version": "0.1.1",
  "bump": "Patch",
  "event_count": 16,
  "started_at": "2026-07-03T14:37:38.910600443Z",
  "ended_at": "2026-07-03T14:37:38.912695924Z",
  "diff": {
    "structural": 0.44272333,
    "api": 0.0,
    "deps": 0.0,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 5,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 0,
    "dependency_nodes": 0,
    "dependency_edges": 0,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.132817,
    "api_breaking": false,
    "api_added": false
  },
  "air_gapped": false
}
```
This file contains a JSON object with various fields, including cluster ID, version, bump type, event count, and timing information. The diff object contains metrics about structural, API, and runtime changes. The weight object contains a score and flags for API breaking and added changes. The air_gapped field is set to false.
<!-- DUMDUM:END 7993302248721001788 -->

<!-- DUMDUM:START 12941938943156646391 -->
## `.kaptaind/analysis/3081d55f-c455-48bc-b11e-00618089bf7d.json`

**File: `.kaptaind/analysis/3081d55f-c455-48bc-b11e-00618089bf7d.json`**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It configures tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters to users or maintainers:**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect:**
The file contains information about the project's analysis, including the cluster ID, version, bump, event count, and diff. The diff section provides details about the structural, API, dependencies, and runtime changes.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**File content:**
```json
{
  "cluster_id": "3081d55f-c455-48bc-b11e-00618089bf7d",
  "version": "0.1.17",
  "bump": "Patch",
  "event_count": 42,
  "started_at": "2026-07-03T14:38:30.408589830Z",
  "ended_at": "2026-07-03T14:38:30.880549025Z",
  "diff": {
    "structural": 0.85353255,
    "api": 0.0,
    "deps": 0.49861115,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 25,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 1,
    "dependency_nodes": 14,
    "dependency_edges": 13,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.355782,
    "api_breaking": false,
    "api_added": false
  },
  "air_gapped": false
}
```

This file appears to be a JSON object containing information about the project's analysis, including the cluster ID, version, bump, event count, and diff. The diff section provides details about the structural, API, dependencies, and runtime changes.
<!-- DUMDUM:END 12941938943156646391 -->

<!-- DUMDUM:START 15693542919906876476 -->
## `.kaptaind/analysis/316c123b-ca9a-4ee3-a62f-2f05b97b6e82.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/316c123b-ca9a-4ee3-a62f-2f05b97b6e82.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is used to configure tooling or runtime behavior, rather than directly serving end-user screens.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project's overall performance.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and timestamps. It also provides details about the diff, weight, and air-gapped status of the project.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. They should also confirm that the explanation still matches the file after major edits, check whether linked commands, images, GIFs, or VHS tapes still exist, and re-run DumDum after the file has rested to ensure generated sections stay aligned.

Here is a summary of the file's content:

| Field | Value |
| --- | --- |
| cluster_id | 316c123b-ca9a-4ee3-a62f-2f05b97b6e82 |
| version | 0.1.52 |
| bump | Patch |
| event_count | 103 |
| started_at | 2026-07-03T14:39:25.374753730Z |
| ended_at | 2026-07-03T14:39:26.479354837Z |
| diff | ... |
| weight | ... |
| air_gapped | false |

Note that the `diff` and `weight` fields contain more detailed information, but it is not included in this summary for brevity.
<!-- DUMDUM:END 15693542919906876476 -->

<!-- DUMDUM:START 1582768753144431548 -->
## `.kaptaind/analysis/35d9392a-93bb-4536-8877-80b0f13990dc.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/35d9392a-93bb-4536-8877-80b0f13990dc.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is one of many JSON files in this directory, each with a unique identifier.

**Why it matters to users or maintainers.**
This file contains information about the analysis of the project, including the version of the tool used, the type of changes made, and the number of events processed. While users may not directly interact with this file, its contents can provide valuable insights into the project's history and behavior.

**User-visible behavior or operational effect.**
The information in this file does not directly affect user-visible behavior or operational effect. However, it can be used by maintainers to understand the project's evolution and make informed decisions about future development.

**Maintainer notes and review checklist.**
Maintainers should review this file periodically to ensure that the information it contains is accurate and up-to-date. They should also check that the file is properly formatted and that any changes made to the project do not break the analysis tool.

Here is a summary of the file's contents:

* `cluster_id`: a unique identifier for the analysis
* `version`: the version of the tool used for analysis
* `bump`: the type of changes made (e.g., patch, minor, major)
* `event_count`: the number of events processed
* `started_at` and `ended_at`: timestamps for the analysis
* `diff`: a detailed breakdown of the changes made, including structural, API, dependencies, and runtime information
* `weight`: a score indicating the significance of the changes
* `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

Overall, this file provides valuable information about the project's analysis and can be used by maintainers to understand the project's evolution and make informed decisions about future development.
<!-- DUMDUM:END 1582768753144431548 -->

<!-- DUMDUM:START 6807260296114610504 -->
## `.kaptaind/analysis/39e0e23d-c16d-400d-8c67-5c4e4e22e847.json`

**File Overview**
===============

**What it is and where it sits in the project.**
--------------------------------------------

The file `.kaptaind/analysis/39e0e23d-c16d-400d-8c67-5c4e4e22e847.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
-----------------------------------------

This file configures tooling or runtime behavior rather than directly serving end-user screens. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**User-visible behavior or operational effect.**
----------------------------------------------

Users may not touch this file directly, but its behavior can still affect reliability, output, or workflow.

**Maintainer notes and review checklist.**
-----------------------------------------

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**File Content**
----------------

The file contains a JSON object with the following properties:

* `cluster_id`: a unique identifier for the cluster
* `version`: the version of the tooling or runtime
* `bump`: the type of version bump (e.g. "Patch")
* `event_count`: the number of events processed
* `started_at` and `ended_at`: timestamps for the start and end of the analysis
* `diff`: an object containing various metrics about the analysis
* `weight`: an object containing a score and flags for API breaking and added changes
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

**Media and demos**
-------------------

No inline GIF, image, or VHS recording references were detected in this snapshot.
<!-- DUMDUM:END 6807260296114610504 -->

<!-- DUMDUM:START 5511275113947963380 -->
## `.kaptaind/analysis/3c909eac-ce73-4996-bb81-d11627e78211.json`

**File: `.kaptaind/analysis/3c909eac-ce73-4996-bb81-d11627e78211.json`**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It contains configuration data for tooling or runtime behavior.

**Why it matters to users or maintainers:**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect:**
The file contains data about the project's analysis, including the cluster ID, version, bump, event count, and timestamps. It also includes information about the diff, weight, and air-gapped status.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**How it works:**
The file contains a JSON object with various properties, including cluster ID, version, bump, event count, and timestamps. The diff property contains information about the structural, API, deps, and runtime changes. The weight property includes a score and information about API breaking and added changes. The air-gapped property indicates whether the analysis is air-gapped or not.

**Media and demos:**
No inline GIF, image, or VHS recording references were detected in this snapshot.

**Example Use Case:**
This file can be used to understand the project's analysis data, including the cluster ID, version, and timestamps. It can also be used to determine the diff, weight, and air-gapped status of the analysis.

**Code Snippet:**
```json
{
  "cluster_id": "3c909eac-ce73-4996-bb81-d11627e78211",
  "version": "0.1.3",
  "bump": "Patch",
  "event_count": 136,
  "started_at": "2026-07-03T14:37:59.235592929Z",
  "ended_at": "2026-07-03T14:38:05.116616133Z",
  "diff": {
    "structural": 0.8941075,
    "api": 0.0,
    "deps": 0.0,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 25,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 0,
    "dependency_nodes": 0,
    "dependency_edges": 0,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.26823226,
    "api_breaking": false,
    "
<!-- DUMDUM:END 5511275113947963380 -->

<!-- DUMDUM:START 15172792040326686712 -->
## `.kaptaind/analysis/3dd6adad-7e48-4eb9-ad34-40e8df51c8d6.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/3dd6adad-7e48-4eb9-ad34-40e8df51c8d6.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains information about the analysis of the project, including the cluster ID, version, bump type, event count, and various metrics about the analysis. This information can be useful for maintainers to understand the project's history and performance.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior or operational effect. However, the information it contains can be used to inform decisions about the project's development and maintenance.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains the following information:
- `cluster_id`: a unique identifier for the analysis cluster
- `version`: the version of the analysis tool
- `bump`: the type of bump (patch, minor, major) that triggered the analysis
- `event_count`: the number of events processed during the analysis
- `started_at` and `ended_at`: timestamps for when the analysis started and ended
- `diff`: a dictionary containing various metrics about the analysis, including structural, API, dependencies, and runtime metrics
- `weight`: a dictionary containing a score and flags for API breaking and added changes
- `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

The file does not contain any references to GIFs, images, or VHS recordings.
<!-- DUMDUM:END 15172792040326686712 -->

<!-- DUMDUM:START 3229152837371928683 -->
## `.kaptaind/analysis/41931d6b-d6f4-4141-87a3-2d135b6f8d31.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/41931d6b-d6f4-4141-87a3-2d135b6f8d31.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters to users or maintainers.**
This file matters because it affects the reliability, output, or workflow of the project. Although users may not touch this file directly, its behavior can still impact the project's overall performance.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and timestamps for the analysis. It also provides details about the diff, weight, and air-gapped status of the project.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash value of 8145108094805285944. Reviewers should confirm that the explanation still matches the file after major edits, check whether linked commands, images, GIFs, or VHS tapes still exist, and re-run DumDum after the file has rested to ensure generated sections stay aligned.

Here is a summary of the file's content:

| Field | Value |
| --- | --- |
| cluster_id | 41931d6b-d6f4-4141-87a3-2d135b6f8d31 |
| version | 0.1.49 |
| bump | Patch |
| event_count | 80 |
| started_at | 2026-07-03T14:39:23.158719724Z |
| ended_at | 2026-07-03T14:39:24.085146949Z |
| diff | ... |
| weight | ... |
| air_gapped | false |

Note that the `diff` and `weight` fields contain more detailed information, but it is not included in this summary for brevity.
<!-- DUMDUM:END 3229152837371928683 -->

<!-- DUMDUM:START 14736448203362531034 -->
## `.kaptaind/analysis/43fed0a2-1fbc-4d09-a99c-10df75a102e9.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/43fed0a2-1fbc-4d09-a99c-10df75a102e9.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Although users may not touch this file directly, its configuration can still impact the project's overall performance.

**User-visible behavior or operational effect.**
The file contains analysis data, including event counts, started and ended timestamps, and a diff object with various metrics. These metrics can be used to understand the project's behavior and performance.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

Here is a breakdown of the JSON content:

- `cluster_id`: A unique identifier for the analysis cluster.
- `version`: The version of the analysis tool.
- `bump`: The type of version bump (Patch, Minor, Major).
- `event_count`: The number of events processed.
- `started_at` and `ended_at`: Timestamps for when the analysis started and ended.
- `diff`: An object containing various metrics, including structural, API, and dependency metrics.
- `weight`: An object containing a score and API breaking/added flags.
- `air_gapped`: A flag indicating whether the analysis was performed in an air-gapped environment.

The `diff` object contains the following metrics:

- `structural`: A structural diff score.
- `api`: An API diff score.
- `deps`: A dependency diff score.
- `runtime`: A runtime diff score.
- `api_breaking`: A flag indicating whether the API is breaking.
- `api_added`: A flag indicating whether the API is added.
- `touched_paths`: The number of touched paths.
- `api_touches`: The number of API touches.
- `api_signatures`: The number of API signatures.
- `dependency_manifests`: The number of dependency manifests.
- `dependency_nodes`: The number of dependency nodes.
- `dependency_edges`: The number of dependency edges.
- `runtime_paths`: The number of runtime paths.
- `bundle`: A bundle diff score.
- `parse_metadata`: An array of parse metadata.
- `ast_cache_hits`: The number of AST cache hits.
- `ast_cache_misses`: The number of AST cache misses.
- `ast_cache_entries`: The number of AST cache entries.
<!-- DUMDUM:END 14736448203362531034 -->

<!-- DUMDUM:START 10422495305547600210 -->
## `.kaptaind/analysis/4652ed92-bccd-4b3a-978f-289c0909e89f.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/4652ed92-bccd-4b3a-978f-289c0909e89f.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains analysis data for the `bart` project, providing insights into the project's structure, dependencies, and performance. While users may not directly interact with this file, its contents can inform decisions about project maintenance, optimization, and future development.

**User-visible behavior or operational effect.**
The data in this file does not directly affect user-visible behavior, but it can influence project operations, such as:

* Identifying performance bottlenecks or areas for optimization
* Informing decisions about code refactoring or restructuring
* Providing insights into dependency management and versioning

**Maintainer notes and review checklist.**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Detailed analysis of the file content.**
The file contains a JSON object with various keys, including:

* `cluster_id`: a unique identifier for the analysis run
* `version`: the version of the `bart` tool used for the analysis
* `bump`: the type of version bump (e.g., patch, minor, major)
* `event_count`: the number of events processed during the analysis
* `started_at` and `ended_at`: timestamps for the start and end of the analysis
* `diff`: an object containing various metrics about the analysis, such as structural changes, API updates, and dependency management
* `weight`: an object containing a score and flags indicating API breaking or added changes
* `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

Overall, this file provides valuable insights into the `bart` project's structure and performance, which can inform maintenance and development decisions.
<!-- DUMDUM:END 10422495305547600210 -->

<!-- DUMDUM:START 17039293228520317552 -->
## `.kaptaind/analysis/56169df8-9234-4b85-b891-f017005852eb.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/56169df8-9234-4b85-b891-f017005852eb.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, which means its behavior can affect the reliability, output, or workflow of the project. Although users may not directly interact with this file, its configuration can still impact the project's performance.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and timestamps for the analysis start and end. It also includes a diff object that provides metrics on the structural, API, dependencies, and runtime changes.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned with the file's changes. The review checklist includes:

* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Additional information.**
The file is 790 bytes in size and contains a JSON object with various fields, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` object provides a detailed analysis of the project's changes, including structural, API, dependencies, and runtime metrics.
<!-- DUMDUM:END 17039293228520317552 -->

<!-- DUMDUM:START 16673816988355320894 -->
## `.kaptaind/analysis/5d7888f9-80eb-4726-a4c5-5a944754cc64.json`

**What it is and where it sits in the project.**

The file `.kaptaind/analysis/5d7888f9-80eb-4726-a4c5-5a944754cc64.json` is a JSON file located in the `.kaptaind/analysis` directory of the project. It contains metadata about a cluster, including its ID, version, and event count.

**Why it matters to users or maintainers.**

This file is part of the project's working contract, and its behavior can affect the reliability, output, or workflow of the project. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect.**

The file contains information about a cluster, including its version, event count, and diff. The diff section provides details about the structural, API, and dependency changes in the cluster. The weight section calculates a score based on the API breaking and added changes.

**Maintainer notes and review checklist.**

Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash value of 8145108094805285944. Reviewers should:

* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Useful relative media links:**

No inline GIF, image, or VHS recording references were detected in this snapshot.
<!-- DUMDUM:END 16673816988355320894 -->

<!-- DUMDUM:START 1496706200811579802 -->
## `.kaptaind/analysis/60a479c2-bcc1-4506-a70e-6f07dcdf8acf.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/60a479c2-bcc1-4506-a70e-6f07dcdf8acf.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains information about the analysis of the project, including the cluster ID, version, bump type, event count, and timing information. It also provides details about the diff between the current and previous versions, including structural, API, dependencies, and runtime changes.

**User-visible behavior or operational effect.**
The information in this file is primarily used for internal analysis and debugging purposes. However, it may be used by maintainers to understand the changes made to the project and to identify potential issues.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains the following information:

- `cluster_id`: A unique identifier for the analysis cluster.
- `version`: The version of the project being analyzed.
- `bump`: The type of bump (patch, minor, major) made to the project.
- `event_count`: The number of events detected during the analysis.
- `started_at` and `ended_at`: The timestamps for when the analysis started and ended.
- `diff`: An object containing information about the diff between the current and previous versions.
- `weight`: An object containing information about the weight of the analysis.
- `air_gapped`: A boolean indicating whether the analysis was performed in an air-gapped environment.

Note that the file does not contain any references to images, GIFs, or VHS tapes.
<!-- DUMDUM:END 1496706200811579802 -->

<!-- DUMDUM:START 10417899345238815831 -->
## `.kaptaind/analysis/64e914c1-6703-4b74-9dee-c12b367415a2.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/64e914c1-6703-4b74-9dee-c12b367415a2.json` is a JSON file located in the `.kaptaind/analysis` directory within the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens. Its behavior can affect reliability, output, or workflow, even if users don't touch it directly.

**User-visible behavior or operational effect.**
The file contains information about the analysis of the project, including the cluster ID, version, bump, event count, and diff. The diff section provides details about the structural, API, dependencies, and runtime changes.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash of 8145108094805285944. Review checklist:

- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

**Media and demos.**
No inline GIF, image, or VHS recording references were detected in this snapshot.

**Example Use Case.**
This file can be used to understand the analysis results of the project, including the changes made and the impact on the project's structure and dependencies. It can also be used to identify potential issues or areas for improvement in the project.

**Related Files.**
Other files in the `.kaptaind/analysis` directory contain similar analysis results, but with different cluster IDs and timestamps. These files can be used to track the evolution of the project over time.

**Future Development.**
In the future, this file can be used as a starting point for more advanced analysis and visualization tools, such as generating reports or dashboards to help maintainers understand the project's structure and dependencies.
<!-- DUMDUM:END 10417899345238815831 -->

<!-- DUMDUM:START 6731411618589587465 -->
## `.kaptaind/analysis/6858827f-a62c-43db-b55d-a8546198592c.json`

**What it is and where it sits in the project.**
This is a JSON file named `6858827f-a62c-43db-b55d-a8546198592c.json` located in the `.kaptaind/analysis` directory within the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens. Its behavior can still affect reliability, output, or workflow, even if users don't touch it directly.

**User-visible behavior or operational effect.**
The file contains metrics and analysis data, including the number of events, started and ended timestamps, and a diff object with various metrics such as structural, API, and dependency changes.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

Here is a summary of the file's content:
```json
{
  "cluster_id": "6858827f-a62c-43db-b55d-a8546198592c",
  "version": "0.1.36",
  "bump": "Patch",
  "event_count": 68,
  "started_at": "2026-07-03T14:38:57.120154988Z",
  "ended_at": "2026-07-03T14:38:57.778945150Z",
  "diff": {
    "structural": 0.85493505,
    "api": 0.0,
    "deps": 0.5861111,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 25,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 2,
    "dependency_nodes": 14,
    "dependency_edges": 13,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.37370273,
    "api_breaking": false,
    "api_added": false
  },
  "air_gapped": false
}
```
Note that the file contains a diff object with various metrics, which suggests that it is used for analyzing changes in the project. The metrics include structural, API, and dependency changes, as well as other information such as touched paths, API touches, and API signatures.
<!-- DUMDUM:END 6731411618589587465 -->

<!-- DUMDUM:START 4110507483602547060 -->
## `.kaptaind/analysis/79a23395-c066-4c97-839e-62bc345ec57b.json`

**What it is and where it sits in the project.**
This is a JSON file named `79a23395-c066-4c97-839e-62bc345ec57b.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens. Its behavior can affect reliability, output, or workflow, even if users don't touch it directly.

**User-visible behavior or operational effect.**
The file contains information about a cluster, including its ID, version, bump type, event count, start and end times, and a diff object with various metrics. The diff object provides insights into the structural, API, dependencies, runtime, and other aspects of the cluster.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

Here is a summary of the file's content:
```json
{
  "cluster_id": "79a23395-c066-4c97-839e-62bc345ec57b",
  "version": "0.1.22",
  "bump": "Patch",
  "event_count": 43,
  "started_at": "2026-07-03T14:38:40.459912259Z",
  "ended_at": "2026-07-03T14:38:40.460813622Z",
  "diff": {
    "structural": 0.85000753,
    "api": 0.0,
    "deps": 0.0,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 25,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 0,
    "dependency_nodes": 0,
    "dependency_edges": 0,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.25500226,
    "api_breaking": false,
    "api_added": false
  },
  "air_gapped": false
}
```
<!-- DUMDUM:END 4110507483602547060 -->

<!-- DUMDUM:START 2432692195260846234 -->
## `.kaptaind/analysis/7cbb8411-2693-46fb-8f7c-65bc4bed838c.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/7cbb8411-2693-46fb-8f7c-65bc4bed838c.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains configuration and analysis data for the project, which can affect the reliability, output, or workflow of the project. Users may not directly interact with this file, but its behavior can still impact the project.

**User-visible behavior or operational effect.**
The file contains data about the project's analysis, including the version, bump type, event count, and analysis results. This data can be used to understand the project's state and make informed decisions.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

Here is a breakdown of the file's content:

- `cluster_id`: a unique identifier for the analysis cluster.
- `version`: the version of the project being analyzed.
- `bump`: the type of bump (e.g., Patch, Minor, Major).
- `event_count`: the number of events analyzed.
- `started_at` and `ended_at`: timestamps for the analysis start and end times.
- `diff`: an object containing analysis results, including structural, API, and runtime differences.
- `weight`: an object containing a score and flags for API breaking and added changes.
- `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment.

Note that the file does not contain any references to images, GIFs, or VHS tapes.
<!-- DUMDUM:END 2432692195260846234 -->

<!-- DUMDUM:START 9704362059076302878 -->
## `.kaptaind/analysis/7e4a0d07-d565-4377-80e1-0cba006ec3ba.json`

**File Overview**

The file `.kaptaind/analysis/7e4a0d07-d565-4377-80e1-0cba006ec3ba.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It has a size of 789 bytes.

**What it is and where it sits in the project**

This file is a JSON configuration file that stores analysis data for the `bart` project. It is part of the `.kaptaind/analysis` directory, which suggests that it is related to the analysis and processing of project data.

**Why it matters to users or maintainers**

This file is important because it contains information about the analysis process, including the version of the analysis, the type of changes detected, and the number of events processed. This information can be useful for maintainers to understand the project's history and make informed decisions about future development.

**User-visible behavior or operational effect**

The contents of this file do not directly affect user-visible behavior, but it can influence the project's maintenance and development workflow. For example, the analysis data can help identify areas that require attention or optimization.

**Maintainer notes and review checklist**

Maintainers should review this file to ensure that the analysis data is accurate and up-to-date. They should also check that the file is properly formatted and that the data is consistent with the project's history.

**Review checklist:**

1. Confirm that the analysis data is accurate and up-to-date.
2. Check that the file is properly formatted.
3. Verify that the data is consistent with the project's history.
4. Re-run the analysis process to ensure that the data is current.

**Additional information**

The file contains a JSON object with several key-value pairs, including:

* `cluster_id`: a unique identifier for the analysis cluster
* `version`: the version of the analysis software
* `bump`: the type of changes detected (in this case, a patch update)
* `event_count`: the number of events processed
* `started_at` and `ended_at`: timestamps for the start and end of the analysis process
* `diff`: an object containing information about the changes detected
* `weight`: an object containing information about the analysis score
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

Overall, this file provides valuable information about the analysis process and can help maintainers understand the project's history and make informed decisions about future development.
<!-- DUMDUM:END 9704362059076302878 -->

<!-- DUMDUM:START 8539796404592737121 -->
## `.kaptaind/analysis/7e6099f4-de05-47b7-a18e-36b1ae02d836.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/7e6099f4-de05-47b7-a18e-36b1ae02d836.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains information about the analysis of the project, including the version of the tool used, the type of bump (Patch, Minor, Major), the number of events, and the start and end times of the analysis. This information can be useful for maintainers to understand the project's history and make informed decisions.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior or operational effect. However, the information it contains can be used to inform decisions that may impact the project's behavior or performance.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the information it contains is accurate and up-to-date. They should also check that the file is properly formatted and that any changes to the file do not break the analysis process.

Here is a summary of the file's contents:

* `cluster_id`: a unique identifier for the analysis
* `version`: the version of the tool used for the analysis
* `bump`: the type of bump (Patch, Minor, Major)
* `event_count`: the number of events in the analysis
* `started_at` and `ended_at`: the start and end times of the analysis
* `diff`: an object containing information about the differences between the analyzed code and the previous version
* `weight`: an object containing information about the weight of the analysis
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

Note that the file contains a lot of technical information that may not be immediately relevant to users. However, it can be useful for maintainers to understand the project's history and make informed decisions.
<!-- DUMDUM:END 8539796404592737121 -->

<!-- DUMDUM:START 16988410031706325473 -->
## `.kaptaind/analysis/80189b65-d7b2-45e1-b7e0-9aaad0582dfd.json`

**.kaptaind/analysis/80189b65-d7b2-45e1-b7e0-9aaad0582dfd.json**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers:**
This file contains information about the analysis of the project, including the version of the tool used, the type of bump (patch, minor, major), and the number of events processed. This information can be useful for understanding the project's history and making informed decisions about future development.

**User-visible behavior or operational effect:**
The contents of this file do not directly affect user-visible behavior, but it can provide insights into the project's history and help maintainers make informed decisions.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**File content:**
The file contains a JSON object with the following properties:

* `cluster_id`: a unique identifier for the analysis cluster
* `version`: the version of the tool used for analysis
* `bump`: the type of bump (patch, minor, major)
* `event_count`: the number of events processed
* `started_at` and `ended_at`: timestamps for the start and end of the analysis
* `diff`: an object containing information about the differences between the analyzed code and the previous version
* `weight`: an object containing information about the weight of the analysis
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

**Media and demos:**
No inline GIF, image, or VHS recording references were detected in this snapshot.
<!-- DUMDUM:END 16988410031706325473 -->

<!-- DUMDUM:START 5478119168844859647 -->
## `.kaptaind/analysis/826ac6d3-3e28-49b2-b988-5d2b1a53b25e.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/826ac6d3-3e28-49b2-b988-5d2b1a53b25e.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains analysis data for the `bart` project, specifically information about the cluster, version, and event count. It also includes details about the analysis process, such as the start and end times, and the differences between the structural, API, dependencies, and runtime aspects of the project.

**User-visible behavior or operational effect.**
The data in this file is likely used by the `bart` project to inform its behavior and decision-making processes. However, the exact impact on user-visible behavior is not immediately clear without further context.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the analysis data is accurate and up-to-date. They should also check that the file is properly formatted and that any changes to the file do not break the analysis process.

Note: The file content is a JSON object with various fields, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` field contains a nested object with various metrics, such as `structural`, `api`, `deps`, and `runtime`. The `weight` field contains a nested object with a `score` field and two boolean fields, `api_breaking` and `api_added`.
<!-- DUMDUM:END 5478119168844859647 -->

<!-- DUMDUM:START 16337895552164349068 -->
## `.kaptaind/analysis/82c2ec14-13b8-4dd7-a9d0-fb6dc8a174f8.json`

**What it is and where it sits in the project.**
This is a JSON file named `82c2ec14-13b8-4dd7-a9d0-fb6dc8a174f8.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains analysis data, which can be useful for understanding the project's behavior, output, or workflow. Although users may not directly interact with this file, its contents can still impact the project's reliability and performance.

**User-visible behavior or operational effect.**
The data in this file is likely used by the project's tooling or runtime to inform decisions or optimize behavior. However, without more context, it's difficult to determine the exact impact on user-visible behavior.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains a JSON object with various fields, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` field contains a nested object with metrics related to structural, API, dependency, and runtime changes. The `weight` field has a `score` value and flags for API breaking and added changes.

Without more context, it's challenging to provide a more detailed explanation of this file's purpose and impact. However, it's clear that this file is an analysis result, and its contents can be useful for understanding the project's behavior and performance.
<!-- DUMDUM:END 16337895552164349068 -->

<!-- DUMDUM:START 3316195894982573062 -->
## `.kaptaind/analysis/85ae96da-8f9a-4095-99c4-6e40214da2e7.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/85ae96da-8f9a-4095-99c4-6e40214da2e7.json` is a JSON file located in the `.kaptaind/analysis` directory within the `bart` project. It appears to be a configuration file for tooling or runtime behavior.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Although users may not touch this file directly, understanding its purpose and behavior is essential for maintaining the project.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and analysis results. The analysis results include metrics such as structural diff, API diff, and dependency analysis.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash value of 8145108094805285944. Reviewers should confirm that the explanation still matches the file after major edits, check whether linked commands, images, GIFs, or VHS tapes still exist, and re-run DumDum after the file has rested to ensure generated sections stay aligned.

**Additional context.**
The file is one of many JSON files in the `.kaptaind/analysis` directory, each with a unique cluster ID. The file's content suggests that it is related to analysis and diffing, possibly used for dependency management or API analysis. The presence of a `weight` section with a score and API breaking/added flags implies that the file is used for some form of prioritization or decision-making.
<!-- DUMDUM:END 3316195894982573062 -->

<!-- DUMDUM:START 532531959930203070 -->
## `.kaptaind/analysis/85d6176e-d67e-4e36-9451-4c35813bbc95.json`

**What it is and where it sits in the project:**
The file `.kaptaind/analysis/85d6176e-d67e-4e36-9451-4c35813bbc95.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers:**
This file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and analysis results. It provides insights into the project's structure, dependencies, and runtime behavior.

**User-visible behavior or operational effect:**
The contents of this file do not directly affect user-visible behavior or operational effects. However, the analysis results may influence future development decisions or maintenance tasks.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

Note: The file contains a JSON object with various properties, including `cluster_id`, `version`, `bump`, `event_count`, and `diff`. The `diff` property contains information about the analysis results, including structural, API, dependencies, and runtime metrics.
<!-- DUMDUM:END 532531959930203070 -->

<!-- DUMDUM:START 9606224565232746554 -->
## `.kaptaind/analysis/89cf3209-9bfc-47e9-b2a1-a47cfb425c4a.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/89cf3209-9bfc-47e9-b2a1-a47cfb425c4a.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It appears to be a configuration file for tooling or runtime behavior.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project's overall performance.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and timestamps. It also includes a diff object with various metrics, such as structural, API, and dependency changes.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. They should also confirm that the explanation still matches the file after major edits and check whether linked commands, images, GIFs, or VHS tapes still exist. Finally, they should re-run DumDum after the file has rested so generated sections stay aligned.

Here is a summary of the file's content:

* Cluster ID: `89cf3209-9bfc-47e9-b2a1-a47cfb425c4a`
* Version: `0.1.18`
* Bump type: `Patch`
* Event count: `119`
* Started at: `2026-07-03T14:38:30.880552144Z`
* Ended at: `2026-07-03T14:38:34.185275951Z`
* Diff:
	+ Structural: `0.87478`
	+ API: `0.0`
	+ Deps: `0.49861115`
	+ Runtime: `0.0`
	+ API breaking: `false`
	+ API added: `false`
	+ Touched paths: `25`
	+ API touches: `0`
	+ API signatures: `0`
	+ Dependency manifests: `1`
	+ Dependency nodes: `14`
	+ Dependency edges: `13`
	+ Runtime paths: `0`
	+ Bundle: `0.0`
	+ Parse metadata: `[]`
	+ AST cache hits: `0`
	+ AST cache misses: `0`
	+ AST cache entries: `0`
* Weight:
	+ Score: `0.36215624`
	+ API breaking: `false`
	+ API added: `false`
* Air gapped: `false`
<!-- DUMDUM:END 9606224565232746554 -->

<!-- DUMDUM:START 376107356206491086 -->
## `.kaptaind/analysis/9276a9fb-4515-4314-8c03-7dcbb1412f66.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/9276a9fb-4515-4314-8c03-7dcbb1412f66.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract and configures tooling or runtime behavior. Although users may not touch this file directly, its behavior can still affect reliability, output, or workflow.

**User-visible behavior or operational effect.**
The file contains information about a cluster, including its ID, version, and bump type. It also provides details about the analysis, such as the event count, started and ended times, and a diff object that contains various metrics.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash of 8145108094805285944. Reviewers should confirm that the explanation still matches the file after major edits, check whether linked commands, images, GIFs, or VHS tapes still exist, and re-run DumDum after the file has rested to ensure generated sections stay aligned.

**Additional context.**
The file is one of many JSON files in the `.kaptaind/analysis` directory, which suggests that it is part of a larger analysis or monitoring system. The presence of a `diff` object and various metrics implies that the file is used to track changes and provide insights into the project's behavior.
<!-- DUMDUM:END 376107356206491086 -->

<!-- DUMDUM:START 7896881624602113619 -->
## `.kaptaind/analysis/9caa3d87-e537-48fa-937d-a7b643fe055f.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/9caa3d87-e537-48fa-937d-a7b643fe055f.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains analysis data, which is crucial for understanding the project's behavior and performance. It provides insights into the project's dependencies, runtime, and structural changes.

**User-visible behavior or operational effect.**
The data in this file can affect the project's reliability, output, and workflow. Users may not directly interact with this file, but its behavior can still impact the project's overall performance.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash of 8145108094805285944. Reviewers should:

* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

This file contains analysis data, including:

* Cluster ID: `9caa3d87-e537-48fa-937d-a7b643fe055f`
* Version: `0.1.51`
* Bump: `Patch`
* Event count: 54
* Started at: `2026-07-03T14:39:24.707306905Z`
* Ended at: `2026-07-03T14:39:25.374753134Z`
* Diff:
	+ Structural: 0.8550025
	+ API: 0.0
	+ Deps: 0.5861111
	+ Runtime: 0.0
	+ API breaking: false
	+ API added: false
	+ Touched paths: 25
	+ API touches: 0
	+ API signatures: 0
	+ Dependency manifests: 2
	+ Dependency nodes: 14
	+ Dependency edges: 13
	+ Runtime paths: 0
	+ Bundle: 0.0
	+ Parse metadata: []
	+ AST cache hits: 0
	+ AST cache misses: 0
	+ AST cache entries: 0
* Weight:
	+ Score: 0.373723
	+ API breaking: false
	+ API added: false
* Air gapped: false
<!-- DUMDUM:END 7896881624602113619 -->

<!-- DUMDUM:START 17068772527362315416 -->
## `.kaptaind/analysis/9d690c3f-5d82-4fec-93ca-cbb31bb61820.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/9d690c3f-5d82-4fec-93ca-cbb31bb61820.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It contains analysis data for a specific cluster.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect.**
The file contains data about a cluster's analysis, including the version, bump type, event count, and diff information. This data can be used to understand the project's behavior and make informed decisions.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

Here is a breakdown of the file's content:

- `cluster_id`: A unique identifier for the cluster.
- `version`: The version of the project.
- `bump`: The type of bump (e.g., Patch, Minor, Major).
- `event_count`: The number of events in the cluster.
- `started_at` and `ended_at`: Timestamps for when the analysis started and ended.
- `diff`: An object containing information about the diff between the previous and current versions.
  - `structural`: The structural diff score.
  - `api`: The API diff score.
  - `deps`: The dependency diff score.
  - `runtime`: The runtime diff score.
  - `api_breaking`: Whether the API is breaking.
  - `api_added`: Whether the API is added.
  - `touched_paths`: The number of touched paths.
  - `api_touches`: The number of API touches.
  - `api_signatures`: The number of API signatures.
  - `dependency_manifests`: The number of dependency manifests.
  - `dependency_nodes`: The number of dependency nodes.
  - `dependency_edges`: The number of dependency edges.
  - `runtime_paths`: The number of runtime paths.
  - `bundle`: The bundle diff score.
  - `parse_metadata`: An array of parse metadata.
  - `ast_cache_hits`: The number of AST cache hits.
  - `ast_cache_misses`: The number of AST cache misses.
  - `ast_cache_entries`: The number of AST cache entries.
- `weight`: An object containing the weight of the cluster.
  - `score`: The score of the cluster.
  - `api_breaking`: Whether the API is breaking.
  - `api_added`: Whether the API is added.
- `air_gapped`: Whether the cluster is air-gapped.
<!-- DUMDUM:END 17068772527362315416 -->

<!-- DUMDUM:START 1574105055212462313 -->
## `.kaptaind/analysis/a099ca29-af06-4095-9a0e-e97e54e01072.json`

**What it is and where it sits in the project.**
This is a JSON file located in the `.kaptaind/analysis` directory of the project. It contains configuration information for tooling or runtime behavior.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect.**
The file contains information about a cluster, including its ID, version, and bump type. It also provides details about the event count, start and end times, and a diff object that contains information about structural, API, dependencies, and runtime changes.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains the following information:

- `cluster_id`: a unique identifier for the cluster
- `version`: the version of the cluster
- `bump`: the type of bump (e.g., Patch)
- `event_count`: the number of events
- `started_at` and `ended_at`: timestamps for the start and end of the cluster
- `diff`: an object containing information about structural, API, dependencies, and runtime changes
- `weight`: an object containing a score and information about API breaking and added changes
- `air_gapped`: a boolean indicating whether the cluster is air-gapped or not

The `diff` object contains the following information:

- `structural`: a score indicating the structural changes
- `api`: a score indicating API changes
- `deps`: a score indicating dependency changes
- `runtime`: a score indicating runtime changes
- `api_breaking`: a boolean indicating whether the API is breaking
- `api_added`: a boolean indicating whether the API is added
- `touched_paths`: the number of touched paths
- `api_touches`: the number of API touches
- `api_signatures`: the number of API signatures
- `dependency_manifests`: the number of dependency manifests
- `dependency_nodes`: the number of dependency nodes
- `dependency_edges`: the number of dependency edges
- `runtime_paths`: the number of runtime paths
- `bundle`: a score indicating bundle changes
- `parse_metadata`: an array of parse metadata
- `ast_cache_hits`: the number of AST cache hits
- `ast_cache_misses`: the number of AST cache misses
- `ast_cache_entries`: the number of AST cache entries
<!-- DUMDUM:END 1574105055212462313 -->

<!-- DUMDUM:START 14339438558634475415 -->
## `.kaptaind/analysis/a4ed4b1a-a3c9-473c-b9aa-663b5b424a43.json`

**What it is and where it sits in the project.**
This is a JSON file named `a4ed4b1a-a3c9-473c-b9aa-663b5b424a43.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains configuration information for tooling or runtime behavior, which can affect the reliability, output, or workflow of the project. Although users may not directly interact with this file, its behavior can still impact the project's overall performance.

**User-visible behavior or operational effect.**
The file's contents are used to determine the version, bump type, event count, and other metrics related to the project's analysis. This information can be used to inform decisions about the project's maintenance and development.

**Maintainer notes and review checklist.**
- Keep the generated explanation aligned when this file changes.
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains metadata about the project's analysis, including the version, bump type, event count, and metrics related to the analysis. The `diff` section provides information about the structural, API, and dependency changes, while the `weight` section contains a score and flags indicating whether the changes are API-breaking or added. The `air_gapped` flag indicates whether the analysis was performed in an air-gapped environment.
<!-- DUMDUM:END 14339438558634475415 -->

<!-- DUMDUM:START 6467777792601310097 -->
## `.kaptaind/analysis/a6185f86-99a8-4c23-a13d-15576dd9f361.json`

- **What it is:** a JSON file at `.kaptaind/analysis/a6185f86-99a8-4c23-a13d-15576dd9f361.json`.
- **Language:** JSON
- **Size:** 34 lines
- **Parser:** heuristic fallback (tree-sitter unavailable or parse rejected)

<!-- needs-review: claim verification failed -->
<!-- DUMDUM:END 6467777792601310097 -->

<!-- DUMDUM:START 7916899506605849766 -->
## `.kaptaind/analysis/af2be5aa-bbf4-4df2-afba-c28743a73d15.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/af2be5aa-bbf4-4df2-afba-c28743a73d15.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains configuration and analysis data for the `bart` project. It provides insights into the project's behavior, output, and workflow, which can be crucial for maintainers to understand and troubleshoot issues.

**User-visible behavior or operational effect.**
The data in this file affects the reliability, output, and workflow of the `bart` project. Users may not directly interact with this file, but its behavior can impact the overall performance and usability of the project.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned with the file's changes. Review the following:

- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

**Additional notes.**
The file contains a JSON object with various settings and analysis data, including:

- `cluster_id`: a unique identifier for the analysis cluster.
- `version`: the version of the `bart` project.
- `bump`: the type of version bump (e.g., Patch).
- `event_count`: the number of events processed.
- `started_at` and `ended_at`: timestamps for the analysis.
- `diff`: a detailed analysis of the project's changes, including structural, API, and dependency metrics.
- `weight`: a score indicating the significance of the analysis.
- `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment.

These settings and data provide valuable insights into the project's behavior and can help maintainers understand and troubleshoot issues.
<!-- DUMDUM:END 7916899506605849766 -->

<!-- DUMDUM:START 1431055682028305102 -->
## `.kaptaind/analysis/b11eba79-6595-4939-91d4-cc76b5730986.json`

**What it is and where it sits in the project.**
This is a JSON file located in the `.kaptaind/analysis` directory. It is one of many files in this directory, which contains a collection of JSON files.

**Why it matters to users or maintainers.**
This file contains information about the analysis of a project, including the version of the analysis, the type of bump (patch, minor, major), and various metrics such as event count, started and ended times, and diff statistics. This information can be useful for maintainers to understand the state of the project and make informed decisions.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior, but the analysis it represents can inform decisions that impact the project's development and maintenance.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains a JSON object with the following properties:

- `cluster_id`: a unique identifier for the analysis
- `version`: the version of the analysis
- `bump`: the type of bump (patch, minor, major)
- `event_count`: the number of events in the analysis
- `started_at` and `ended_at`: timestamps for when the analysis started and ended
- `diff`: an object containing various metrics about the analysis, including structural, API, dependencies, and runtime differences
- `weight`: an object containing a score and flags for API breaking and added changes
- `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment
<!-- DUMDUM:END 1431055682028305102 -->

<!-- DUMDUM:START 10294918923619723653 -->
## `.kaptaind/analysis/b1ff7acd-af35-4e58-b4f3-cf0c06584e31.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/b1ff7acd-af35-4e58-b4f3-cf0c06584e31.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is a configuration file that stores analysis data for the project.

**Why it matters to users or maintainers.**
This file is important because it contains information about the project's analysis, including the version of the analysis, the type of changes made, and the number of events processed. This information can be useful for maintainers to understand the project's history and make informed decisions about future development.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior, but it can provide insights into the project's analysis history, which can be useful for troubleshooting or understanding the project's evolution.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the analysis data is accurate and up-to-date. They should also check that the file is properly formatted and that the data is consistent with the project's history. The review checklist should include:

* Confirm that the file is properly formatted and that the data is consistent with the project's history.
* Verify that the analysis data is accurate and up-to-date.
* Check that the file is properly linked to other relevant files and directories.
* Review the file's contents to ensure that it is relevant and useful for understanding the project's history.

**Additional notes.**
The file contains information about the project's analysis, including the version of the analysis, the type of changes made, and the number of events processed. The file also contains a `diff` object that provides information about the changes made to the project's code. The `weight` object provides a score that indicates the significance of the changes made. The file also contains a `started_at` and `ended_at` field that indicates the time period during which the analysis was performed.
<!-- DUMDUM:END 10294918923619723653 -->

<!-- DUMDUM:START 8758610593007447801 -->
## `.kaptaind/analysis/b39b69cf-6a51-4371-8b5f-63490e0d59ff.json`

**What it is and where it sits in the project.**
This is a JSON file named `b39b69cf-6a51-4371-8b5f-63490e0d59ff.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains analysis data that helps maintainers understand the project's behavior, output, and workflow. Although users may not directly interact with this file, its behavior can still impact the project's reliability and performance.

**User-visible behavior or operational effect.**
The data in this file is likely used by the `kaptaind` tool to analyze and report on the project's behavior. The analysis data includes metrics such as event count, started and ended timestamps, and structural, API, and dependency metrics.

**Maintainer notes and review checklist.**
Maintainers should review this file periodically to ensure that the analysis data is accurate and up-to-date. They should also check that the linked commands, images, and VHS tapes still exist and are relevant to the project.

**Additional notes.**
The file contains a JSON object with various fields, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` field contains a nested object with metrics such as structural, API, and dependency metrics. The `weight` field contains a nested object with a `score` field and `api_breaking` and `api_added` fields.

**Review checklist:**

* Confirm that the analysis data is accurate and up-to-date.
* Check that the linked commands, images, and VHS tapes still exist and are relevant to the project.
* Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 8758610593007447801 -->

<!-- DUMDUM:START 14559793238528076758 -->
## `.kaptaind/analysis/b5c3f66c-b849-4b98-b408-ac938935697d.json`

**What it is and where it sits in the project.**
This is a JSON file named `b5c3f66c-b849-4b98-b408-ac938935697d.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains configuration and analysis data for the `bart` project. It provides insights into the project's tooling and runtime behavior, which can affect reliability, output, or workflow.

**User-visible behavior or operational effect.**
Users may not directly interact with this file, but its behavior can still impact the project's performance and output. The data in this file can be used to inform decisions about the project's development and maintenance.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned with the file's changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash of 8145108094805285944. Reviewers should:

* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains analysis data, including:

* `cluster_id`: a unique identifier for the analysis cluster.
* `version`: the version of the `bart` tool.
* `bump`: the type of version bump (e.g., patch, minor, major).
* `event_count`: the number of events processed during the analysis.
* `started_at` and `ended_at`: timestamps for the analysis start and end times.
* `diff`: a detailed analysis of the project's changes, including structural, API, dependency, and runtime metrics.
* `weight`: a weighted score for the analysis, including API breaking and added metrics.
* `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment.

This file is an essential part of the `bart` project's working contract, and its behavior can impact the project's reliability, output, and workflow. Maintainers should carefully review and maintain this file to ensure the project's continued success.
<!-- DUMDUM:END 14559793238528076758 -->

<!-- DUMDUM:START 16002343091053906748 -->
## `.kaptaind/analysis/b5c436cd-923c-4fed-b49c-9c5db2140f94.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/b5c436cd-923c-4fed-b49c-9c5db2140f94.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is one of many similar files in this directory, which suggests that it is used for analysis or processing of some kind.

**Why it matters to users or maintainers.**
This file is likely used by the `kaptaind` tool to store analysis results or metadata. While users may not directly interact with this file, its contents can affect the behavior or output of the tool, which can impact the reliability or workflow of the project.

**User-visible behavior or operational effect.**
The file contains information about a cluster, including its ID, version, and bump type. It also includes metrics such as event count, started and ended times, and a diff object that contains various statistics about the cluster. These metrics can be used to monitor the health or performance of the cluster.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the analysis results or metadata are accurate and up-to-date. They should also check that the file is properly formatted and that any linked commands, images, or VHS tapes still exist. If the file changes significantly, the explanation should be updated to reflect the new information.

**Additional information.**
The file contains a diff object that includes metrics such as structural, API, and dependency changes. These metrics can be used to monitor the health or performance of the cluster. The file also includes a weight object that contains a score and API breaking/added flags.

**Media and demos.**
No inline GIF, image, or VHS recording references were detected in this snapshot.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 16002343091053906748 -->

<!-- DUMDUM:START 18336670138855935555 -->
## `.kaptaind/analysis/b744be03-d46e-4164-8868-5df42c58957a.json`

**What it is and where it sits in the project.**
This is a JSON file named `b744be03-d46e-4164-8868-5df42c58957a.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains analysis data and is part of the project's working contract. Its behavior can affect reliability, output, or workflow, even if users don't touch it directly.

**User-visible behavior or operational effect.**
The file's contents are not directly visible to users, but its analysis data can influence the project's behavior and output.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

**File contents.**
The file contains the following JSON data:
```json
{
  "cluster_id": "b744be03-d46e-4164-8868-5df42c58957a",
  "version": "0.1.24",
  "bump": "Patch",
  "event_count": 60,
  "started_at": "2026-07-03T14:38:42.529193712Z",
  "ended_at": "2026-07-03T14:38:45.302846977Z",
  "diff": {
    "structural": 0.8707975,
    "api": 0.0,
    "deps": 0.0,
    "runtime": 0.0,
    "api_breaking": false,
    "api_added": false,
    "touched_paths": 25,
    "api_touches": 0,
    "api_signatures": 0,
    "dependency_manifests": 0,
    "dependency_nodes": 0,
    "dependency_edges": 0,
    "runtime_paths": 0,
    "bundle": 0.0,
    "parse_metadata": [],
    "ast_cache_hits": 0,
    "ast_cache_misses": 0,
    "ast_cache_entries": 0
  },
  "weight": {
    "score": 0.26123926,
    "api_breaking": false,
    "api_added": false
  },
  "air_gapped": false
}
```
This data includes analysis metrics, such as the number of events, structural changes, and API-related metrics.
<!-- DUMDUM:END 18336670138855935555 -->

<!-- DUMDUM:START 9167267837236243452 -->
## `.kaptaind/analysis/c82668b4-4835-4252-9175-b5f4c44bb8a0.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/c82668b4-4835-4252-9175-b5f4c44bb8a0.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains information about the project's analysis, including the version, bump type, event count, and diff statistics. It provides insights into the project's behavior and can be useful for maintainers to understand the project's evolution.

**User-visible behavior or operational effect.**
The information in this file does not directly affect user-visible behavior, but it can influence the project's development and maintenance decisions.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the information is accurate and up-to-date. They should also check that the file is properly formatted and that the data is consistent with the project's expectations.

Here are some specific notes and review items:

* Review the file's content to ensure that it accurately reflects the project's analysis.
* Check that the file is properly formatted and that the data is consistent with the project's expectations.
* Verify that the file is up-to-date and that the information is accurate.
* Review the file's history to ensure that it has not been modified in a way that affects the project's behavior.

Note: The file content is a JSON object that contains information about the project's analysis. The object has several properties, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` property contains information about the project's structural, API, and runtime changes, while the `weight` property provides a score and API breaking information.
<!-- DUMDUM:END 9167267837236243452 -->

<!-- DUMDUM:START 4677604962361480330 -->
## `.kaptaind/analysis/c90ba5bd-5656-4ae7-8032-406520e01f9c.json`

**Documentation for `.kaptaind/analysis/c90ba5bd-5656-4ae7-8032-406520e01f9c.json`**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It contains analysis data for a specific cluster.

**Why it matters to users or maintainers:**
This file provides insights into the analysis process, including the cluster ID, version, bump type, event count, and timing information. It also includes a diff object that provides details about the structural, API, dependencies, and runtime changes.

**User-visible behavior or operational effect:**
Users may not directly interact with this file, but its contents can influence the reliability, output, or workflow of the `bart` tool.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**How it works:**
The file contains a JSON object with various fields, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, and `diff`. The `diff` object provides detailed information about the changes detected during the analysis process.

**Media and demos:**
No inline GIF, image, or VHS recording references were detected in this snapshot.

**Example Use Case:**
This file can be used by maintainers to understand the analysis process and identify potential issues or areas for improvement. By analyzing the `diff` object, maintainers can gain insights into the changes detected during the analysis process and make informed decisions about the project's development.
<!-- DUMDUM:END 4677604962361480330 -->

<!-- DUMDUM:START 11897118690686743790 -->
## `.kaptaind/analysis/ca7e3a2a-62e0-4725-93a5-184d475b8eaf.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/ca7e3a2a-62e0-4725-93a5-184d475b8eaf.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens. Its behavior can still affect reliability, output, or workflow, even if users do not touch it directly.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and analysis results. This information can be used to understand the project's state and make informed decisions.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

**Detailed Analysis**
The file contains a JSON object with several key-value pairs. The `cluster_id` field identifies the cluster, while the `version` field indicates the version of the analysis. The `bump` field specifies the type of bump (Patch, Minor, or Major), and the `event_count` field shows the number of events processed.

The `started_at` and `ended_at` fields indicate the start and end times of the analysis, respectively. The `diff` field contains information about the analysis results, including structural, API, dependencies, and runtime differences.

The `weight` field provides a score indicating the significance of the analysis results, while the `air_gapped` field indicates whether the analysis was performed in an air-gapped environment.

**Media and demos**
No inline GIF, image, or VHS recording references were detected in this snapshot.
<!-- DUMDUM:END 11897118690686743790 -->

<!-- DUMDUM:START 812170418940445174 -->
## `.kaptaind/analysis/ce9f36f4-92d2-489a-9405-cfe9edf84b28.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/ce9f36f4-92d2-489a-9405-cfe9edf84b28.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains information about the analysis of the project, including the version of the analysis, the type of bump (Patch), the number of events, and the start and end times of the analysis. This information can be useful for maintainers to understand the history of the project and make informed decisions about future development.

**User-visible behavior or operational effect.**
This file does not directly affect user-visible behavior, but its contents can influence the decisions made by maintainers, which in turn can impact the project's development and maintenance.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the information it contains is accurate and up-to-date. They should also check that the file is properly formatted and that its contents are consistent with the project's overall development strategy.

Here is a summary of the file's contents:

* `cluster_id`: a unique identifier for the analysis
* `version`: the version of the analysis
* `bump`: the type of bump (Patch)
* `event_count`: the number of events in the analysis
* `started_at` and `ended_at`: the start and end times of the analysis
* `diff`: an object containing information about the differences between the analyzed code and the previous version
* `weight`: an object containing information about the weight of the analysis
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

Note that the file does not contain any references to images, GIFs, or VHS recordings.
<!-- DUMDUM:END 812170418940445174 -->

<!-- DUMDUM:START 15769374764083763590 -->
## `.kaptaind/analysis/d36405a2-13ff-41a4-a544-0f10c4b623d1.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/d36405a2-13ff-41a4-a544-0f10c4b623d1.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains configuration and analysis data for the project, which can affect the reliability, output, and workflow of the project. Although users may not directly interact with this file, its behavior can still impact the project's overall performance.

**User-visible behavior or operational effect.**
The file's content is related to the project's analysis and configuration, which can influence the project's output and behavior. However, the specific effects are not directly visible to users, as they are primarily concerned with the project's end-user screens.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned with the file's changes. The review checklist includes:

- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains configuration data, including:

- `cluster_id`: a unique identifier for the cluster
- `version`: the version of the project
- `bump`: the type of version bump (e.g., Patch)
- `event_count`: the number of events processed
- `started_at` and `ended_at`: timestamps for the analysis
- `diff`: a detailed analysis of the project's changes
- `weight`: a score indicating the significance of the changes
- `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

Maintainers should review the file's content to ensure it accurately reflects the project's configuration and analysis data.
<!-- DUMDUM:END 15769374764083763590 -->

<!-- DUMDUM:START 3805160665068753587 -->
## `.kaptaind/analysis/d526aea9-fbeb-401a-84de-93dea41863dc.json`

**.kaptaind/analysis/d526aea9-fbeb-401a-84de-93dea41863dc.json**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It contains configuration data for tooling or runtime behavior.

**Why it matters to users or maintainers:**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still have an impact.

**User-visible behavior or operational effect:**
The file contains data about the project's analysis, including the version, bump type, event count, and diff information. This data can be used to understand the project's history and changes.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**File content:**
The file contains a JSON object with the following properties:

* `cluster_id`: a unique identifier for the analysis cluster
* `version`: the version of the analysis tool
* `bump`: the type of bump (Patch, Minor, Major)
* `event_count`: the number of events in the analysis
* `started_at` and `ended_at`: timestamps for the start and end of the analysis
* `diff`: an object containing information about the differences between the analyzed files
* `weight`: an object containing a score and information about API breaking and added changes
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

The `diff` object contains several properties, including:

* `structural`: a score indicating the structural changes in the analyzed files
* `api`: a score indicating the API changes in the analyzed files
* `deps`: a score indicating the dependency changes in the analyzed files
* `runtime`: a score indicating the runtime changes in the analyzed files
* `api_breaking`: a boolean indicating whether the API changes are breaking
* `api_added`: a boolean indicating whether new API changes were added
* `touched_paths`: the number of paths that were touched during the analysis
* `api_touches`: the number of API touches during the analysis
* `api_signatures`: the number of API signatures during the analysis
* `dependency_manifests`: the number of dependency manifests during the analysis
* `dependency_nodes`: the number of dependency nodes during the analysis
* `dependency_edges`: the number of dependency edges during the analysis
* `runtime_paths`: the number of runtime paths during the analysis
* `bundle`: a score indicating the bundle changes in the analyzed files
* `parse_metadata`: an array of metadata about the parsed files
* `ast_cache_hits`: the number of AST cache hits during the analysis
* `ast_cache_misses`: the number of AST cache misses during the analysis
* `ast_cache_entries`: the number of AST cache entries during the analysis
<!-- DUMDUM:END 3805160665068753587 -->

<!-- DUMDUM:START 9981474008883723615 -->
## `.kaptaind/analysis/d9dc6bf3-f172-4c0d-8047-344b102bd368.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/d9dc6bf3-f172-4c0d-8047-344b102bd368.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It appears to be a configuration file for tooling or runtime behavior.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect.**
The file contains information about the project's version, bump type, event count, and analysis results. It also includes metrics such as structural, API, and dependency analysis.

**Maintainer notes and review checklist.**
Maintainers should keep the generated explanation aligned when this file changes. The current snapshot has 34 lines, 0 detected function-like definitions, and a hash value of 8145108094805285944. Reviewers should confirm that the explanation still matches the file after major edits, check whether linked commands, images, GIFs, or VHS tapes still exist, and re-run DumDum after the file has rested to ensure generated sections stay aligned.

**Additional notes.**
The file contains a JSON object with various fields, including `cluster_id`, `version`, `bump`, `event_count`, `started_at`, `ended_at`, `diff`, `weight`, and `air_gapped`. The `diff` field contains metrics about structural, API, and dependency analysis, while the `weight` field provides a score and API breaking/added information.
<!-- DUMDUM:END 9981474008883723615 -->

<!-- DUMDUM:START 15284362656725840479 -->
## `.kaptaind/analysis/de5260f4-caa3-4a94-8158-649d8eb9fcbd.json`

**What it is and where it sits in the project.**
This is a JSON file named `de5260f4-caa3-4a94-8158-649d8eb9fcbd.json` located in the `.kaptaind/analysis` directory of the `bart` project.

**Why it matters to users or maintainers.**
This file contains metadata about the analysis of the project, including the version of the tool used, the type of bump (Patch, Minor, Major), and the number of events processed. It also includes information about the time the analysis started and ended, as well as the differences found in the project's structure.

**User-visible behavior or operational effect.**
The information in this file is primarily used by maintainers to understand the history of the project and make informed decisions about future development. However, users may not directly interact with this file.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

Here's a breakdown of the file's content:

- `cluster_id`: A unique identifier for the analysis.
- `version`: The version of the tool used for the analysis.
- `bump`: The type of bump (Patch, Minor, Major) that occurred.
- `event_count`: The number of events processed during the analysis.
- `started_at` and `ended_at`: The timestamps for when the analysis started and ended.
- `diff`: An object containing information about the differences found in the project's structure.
  - `structural`: A measure of the structural changes in the project.
  - `api`: A measure of the API changes in the project.
  - `deps`: A measure of the dependency changes in the project.
  - `runtime`: A measure of the runtime changes in the project.
  - `api_breaking`: A boolean indicating whether the API changes were breaking.
  - `api_added`: A boolean indicating whether new API was added.
  - `touched_paths`: The number of paths that were touched during the analysis.
  - `api_touches`: The number of API touches during the analysis.
  - `api_signatures`: The number of API signatures during the analysis.
  - `dependency_manifests`: The number of dependency manifests during the analysis.
  - `dependency_nodes`: The number of dependency nodes during the analysis.
  - `dependency_edges`: The number of dependency edges during the analysis.
  - `runtime_paths`: The number of runtime paths during the analysis.
  - `bundle`: A measure of the bundle changes in the project.
  - `parse_metadata`: An array of parse metadata.
  - `ast_cache_hits`: The number of AST cache hits.
  - `ast_cache_misses`: The number of AST cache misses.
  - `ast_cache_entries`: The number of AST cache entries.
- `weight`: An object containing information about the weight of the analysis.
  - `score`: A measure of the weight of the analysis.
  - `api_breaking`: A boolean indicating whether the API changes were breaking.
  - `api_added`: A boolean indicating whether new API was added.
- `air_gapped`: A boolean indicating whether the analysis was air-gapped.
<!-- DUMDUM:END 15284362656725840479 -->

<!-- DUMDUM:START 16397675797907236150 -->
## `.kaptaind/analysis/e1d79b62-5210-4e80-8361-f53193a9c23a.json`

**What it is and where it sits in the project.**
This is a JSON file located in the `.kaptaind/analysis` directory of the project. It contains configuration data for tooling or runtime behavior.

**Why it matters to users or maintainers.**
This file is part of the project's working contract, and its behavior can affect reliability, output, or workflow. Users may not touch this file directly, but its behavior can still impact the project.

**User-visible behavior or operational effect.**
The file contains data about a cluster, including its ID, version, and bump type. It also provides information about the event count, start and end times, and a diff object that contains metrics about the cluster's structural, API, and runtime changes.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains the following data:

- `cluster_id`: a unique identifier for the cluster
- `version`: the version of the cluster
- `bump`: the type of bump (Patch, Minor, Major)
- `event_count`: the number of events in the cluster
- `started_at` and `ended_at`: timestamps for the start and end of the cluster
- `diff`: an object containing metrics about the cluster's changes
  - `structural`: a metric about structural changes
  - `api`: a metric about API changes
  - `deps`: a metric about dependency changes
  - `runtime`: a metric about runtime changes
  - `api_breaking`: a boolean indicating whether the API is breaking
  - `api_added`: a boolean indicating whether the API is added
  - `touched_paths`: the number of touched paths
  - `api_touches`: the number of API touches
  - `api_signatures`: the number of API signatures
  - `dependency_manifests`: the number of dependency manifests
  - `dependency_nodes`: the number of dependency nodes
  - `dependency_edges`: the number of dependency edges
  - `runtime_paths`: the number of runtime paths
  - `bundle`: a metric about bundling
  - `parse_metadata`: an array of parse metadata
  - `ast_cache_hits`: the number of AST cache hits
  - `ast_cache_misses`: the number of AST cache misses
  - `ast_cache_entries`: the number of AST cache entries
- `weight`: an object containing a score and API breaking/added flags
- `air_gapped`: a boolean indicating whether the cluster is air-gapped
<!-- DUMDUM:END 16397675797907236150 -->

<!-- DUMDUM:START 9743988560929299077 -->
## `.kaptaind/analysis/e2654113-876b-4556-a7b6-91c232111371.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/e2654113-876b-4556-a7b6-91c232111371.json` is a JSON file located in the `.kaptaind/analysis` directory within the `bart` project. It is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters to users or maintainers.**
This file matters because its behavior can affect reliability, output, or workflow, even if users do not touch it directly. Maintainers should keep the generated explanation aligned when this file changes.

**User-visible behavior or operational effect.**
The file contains information about the project's analysis, including the cluster ID, version, bump type, event count, and analysis results. The analysis results include metrics such as structural, API, dependencies, and runtime changes.

**Maintainer notes and review checklist.**
Maintainers should:

* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Example analysis results.**
The file contains the following analysis results:
```json
"diff": {
  "structural": 0.85588,
  "api": 0.0,
  "deps": 0.5861111,
  "runtime": 0.0,
  "api_breaking": false,
  "api_added": false,
  "touched_paths": 25,
  "api_touches": 0,
  "api_signatures": 0,
  "dependency_manifests": 2,
  "dependency_nodes": 14,
  "dependency_edges": 13,
  "runtime_paths": 0,
  "bundle": 0.0,
  "parse_metadata": [],
  "ast_cache_hits": 0,
  "ast_cache_misses": 0,
  "ast_cache_entries": 0
}
```
These results provide insights into the project's analysis, including structural, API, dependencies, and runtime changes.
<!-- DUMDUM:END 9743988560929299077 -->

<!-- DUMDUM:START 13069135566749028466 -->
## `.kaptaind/analysis/e42b9c44-0232-4d4f-a3da-0ffcf233f1db.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/e42b9c44-0232-4d4f-a3da-0ffcf233f1db.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains information about the analysis of the project, including the version of the tool used, the type of event that occurred, and the time it started and ended. This information can be useful for maintainers to understand the history of the project and make informed decisions about future development.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior or operational effect, as it is primarily used for internal analysis and tracking.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the information it contains is accurate and up-to-date. They should also check that the file is properly formatted and that any changes to the project do not affect the analysis results.

Here is a summary of the file's contents:

* `cluster_id`: a unique identifier for the analysis
* `version`: the version of the tool used for analysis
* `bump`: the type of event that occurred (in this case, a patch update)
* `event_count`: the number of events that occurred during analysis
* `started_at` and `ended_at`: the start and end times of the analysis
* `diff`: an object containing information about the differences between the analyzed code and the previous version
* `weight`: an object containing information about the weight of the analysis results
* `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

Note that the file contains a large amount of technical information that may not be immediately relevant to users or maintainers. However, it can be useful for understanding the history and development of the project.
<!-- DUMDUM:END 13069135566749028466 -->

<!-- DUMDUM:START 7079407096672288819 -->
## `.kaptaind/analysis/e7f51231-94cb-4ade-af6b-1d830f086b69.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/e7f51231-94cb-4ade-af6b-1d830f086b69.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains configuration and analysis data for the project. It provides insights into the project's structure, dependencies, and runtime behavior. Maintainers can use this information to identify potential issues, optimize performance, and make informed decisions about the project's development.

**User-visible behavior or operational effect.**
The data in this file does not directly affect user-visible behavior. However, it can influence the project's reliability, output, and workflow. Users may not interact with this file directly, but its behavior can still impact their experience.

**Maintainer notes and review checklist.**
Maintainers should review this file regularly to ensure that the analysis data is accurate and up-to-date. They should also check for any changes to the file's structure or content that may affect the project's behavior.

Here are some specific review checklist items:

* Confirm that the file's structure and content are consistent with the project's requirements.
* Verify that the analysis data is accurate and reflects the current state of the project.
* Check for any changes to the file's version or bump status.
* Review the event count and started/ended timestamps to ensure that the analysis is up-to-date.
* Inspect the diff data to identify any structural, API, or dependency changes.
* Verify that the weight score and API breaking/added flags are accurate.

By following these review checklist items, maintainers can ensure that the `.kaptaind/analysis/e7f51231-94cb-4ade-af6b-1d830f086b69.json` file remains accurate and useful for project development and maintenance.
<!-- DUMDUM:END 7079407096672288819 -->

<!-- DUMDUM:START 13942265653722645143 -->
## `.kaptaind/analysis/e9d12405-54c9-4aba-bd4a-b1b2c230ddbe.json`

**Documentation depth:** brief explanation, target 260-380 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- Maintainer notes and review checklist.

**What it is:** This is a JSON file in `bart`. It configures tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters:** It configures tooling or runtime behavior rather than directly serving end-user screens. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Users may not touch this file directly, but its behavior can still affect reliability, output, or workflow.

**How it works:** The first meaningful line and surrounding directory are the strongest signals for this file. If that signal is weak, inspect imports, callers, or links before treating the explanation as complete.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 34 lines, 0 detected function-like definitions, hash 8092183528430398516.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 13942265653722645143 -->

<!-- DUMDUM:START 7440883670777085211 -->
## `.kaptaind/analysis/ee10746a-c6ed-4ea0-8049-fb2e40240cb2.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/ee10746a-c6ed-4ea0-8049-fb2e40240cb2.json` is a JSON file located in the `.kaptaind/analysis` directory of the project.

**Why it matters to users or maintainers.**
This file contains metadata about a cluster, including its ID, version, and event count. It also provides information about the analysis process, such as the start and end times, and the types of changes detected.

**User-visible behavior or operational effect.**
This file does not directly affect user-visible behavior or operational effects. However, its contents may be used by other tools or scripts to inform their decisions or actions.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains a JSON object with the following properties:

- `cluster_id`: a unique identifier for the cluster
- `version`: the version of the analysis tool
- `bump`: the type of change detected (in this case, a patch update)
- `event_count`: the number of events detected during the analysis
- `started_at` and `ended_at`: the start and end times of the analysis
- `diff`: an object containing information about the changes detected, including structural, API, and dependency changes
- `weight`: an object containing a score and information about API breaking and added changes
- `air_gapped`: a boolean indicating whether the analysis was performed in an air-gapped environment

The `diff` object contains several properties that provide insight into the changes detected during the analysis. These include:

- `structural`: the percentage of structural changes detected
- `api`: the percentage of API changes detected
- `deps`: the percentage of dependency changes detected
- `runtime`: the percentage of runtime changes detected
- `api_breaking`: a boolean indicating whether any API-breaking changes were detected
- `api_added`: a boolean indicating whether any new APIs were added
- `touched_paths`: the number of paths that were touched during the analysis
- `api_touches`: the number of API touches detected
- `api_signatures`: the number of API signatures detected
- `dependency_manifests`: the number of dependency manifests detected
- `dependency_nodes`: the number of dependency nodes detected
- `dependency_edges`: the number of dependency edges detected
- `runtime_paths`: the number of runtime paths detected
- `bundle`: the percentage of bundle changes detected
- `parse_metadata`: an array of parse metadata
- `ast_cache_hits`: the number of AST cache hits
- `ast_cache_misses`: the number of AST cache misses
- `ast_cache_entries`: the number of AST cache entries
<!-- DUMDUM:END 7440883670777085211 -->

<!-- DUMDUM:START 9615853393932375206 -->
## `.kaptaind/analysis/ef65a75d-c6ab-4fa1-b21d-30f3a94e6749.json`

**Documentation depth:** brief explanation, target 260-380 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- Maintainer notes and review checklist.

**What it is:** This is a JSON file in `bart`. It configures tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters:** It configures tooling or runtime behavior rather than directly serving end-user screens. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Users may not touch this file directly, but its behavior can still affect reliability, output, or workflow.

**How it works:** The first meaningful line and surrounding directory are the strongest signals for this file. If that signal is weak, inspect imports, callers, or links before treating the explanation as complete.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 34 lines, 0 detected function-like definitions, hash 2477185836712239582.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 9615853393932375206 -->

<!-- DUMDUM:START 16375143248312578141 -->
## `.kaptaind/analysis/faca43da-efc8-4d9e-a80d-4a2184b66a01.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/faca43da-efc8-4d9e-a80d-4a2184b66a01.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is a configuration file that stores analysis data.

**Why it matters to users or maintainers.**
This file is important because it contains information about the analysis process, including the version of the tool, the type of bump (Patch, Minor, or Major), and the number of events processed. This information can be useful for understanding the project's history and making informed decisions about future development.

**User-visible behavior or operational effect.**
The contents of this file do not directly affect user-visible behavior, but it can provide insights into the project's history and help maintainers make informed decisions.

**Maintainer notes and review checklist.**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file contains a JSON object with various fields, including:

- `cluster_id`: a unique identifier for the analysis cluster
- `version`: the version of the tool used for analysis
- `bump`: the type of bump (Patch, Minor, or Major)
- `event_count`: the number of events processed
- `started_at` and `ended_at`: timestamps for the start and end of the analysis
- `diff`: an object containing various metrics about the analysis, including structural, API, and runtime differences
- `weight`: an object containing a score and flags for API breaking and added changes
- `air_gapped`: a flag indicating whether the analysis was performed in an air-gapped environment

The `diff` object contains several metrics, including:

- `structural`: a measure of structural changes
- `api`: a measure of API changes
- `deps`: a measure of dependency changes
- `runtime`: a measure of runtime changes
- `api_breaking`: a flag indicating whether the API is breaking
- `api_added`: a flag indicating whether new API is added
- `touched_paths`: the number of touched paths
- `api_touches`: the number of API touches
- `api_signatures`: the number of API signatures
- `dependency_manifests`: the number of dependency manifests
- `dependency_nodes`: the number of dependency nodes
- `dependency_edges`: the number of dependency edges
- `runtime_paths`: the number of runtime paths
- `bundle`: a measure of bundle changes
- `parse_metadata`: an array of parse metadata
- `ast_cache_hits`: the number of AST cache hits
- `ast_cache_misses`: the number of AST cache misses
- `ast_cache_entries`: the number of AST cache entries
<!-- DUMDUM:END 16375143248312578141 -->

<!-- DUMDUM:START 13328138845125072231 -->
## `.kaptaind/analysis/fcebe2fb-9a7d-4c1b-9e00-2b6c2003cf72.json`

**.kaptaind/analysis/fcebe2fb-9a7d-4c1b-9e00-2b6c2003cf72.json**

**What it is and where it sits in the project:**
This is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is a configuration file that stores information about the project's analysis.

**Why it matters to users or maintainers:**
This file is important because it contains information about the project's analysis, such as the version of the analysis, the type of changes made, and the number of events processed. This information can be useful for understanding the project's history and making informed decisions about future development.

**User-visible behavior or operational effect:**
The information in this file does not directly affect the user's experience, but it can be used to inform the user about the project's history and analysis.

**Maintainer notes and review checklist:**

* Keep the generated explanation aligned when this file changes.
* Confirm the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Detailed explanation:**
This JSON file contains information about the project's analysis, including the version of the analysis, the type of changes made, and the number of events processed. The file is organized into several sections, including:

* `cluster_id`: a unique identifier for the analysis cluster
* `version`: the version of the analysis
* `bump`: the type of changes made (e.g. "Patch")
* `event_count`: the number of events processed
* `started_at` and `ended_at`: the start and end times of the analysis
* `diff`: a section that contains information about the changes made, including the structural, API, dependencies, and runtime changes
* `weight`: a section that contains information about the weight of the changes, including the score and API breaking/added flags
* `air_gapped`: a flag that indicates whether the analysis was performed in an air-gapped environment

Overall, this file provides a detailed record of the project's analysis and can be used to inform future development decisions.
<!-- DUMDUM:END 13328138845125072231 -->

<!-- DUMDUM:START 15971414955467102700 -->
## `.kaptaind/analysis/ff55fd57-3516-4cca-89a9-7da268899136.json`

**What it is and where it sits in the project.**
The file `.kaptaind/analysis/ff55fd57-3516-4cca-89a9-7da268899136.json` is a JSON file located in the `.kaptaind/analysis` directory of the `bart` project. It is part of the project's working contract, configuring tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters to users or maintainers.**
This file matters to users or maintainers because its behavior can affect reliability, output, or workflow, even if users do not touch it directly. Maintainers should keep the generated explanation aligned when this file changes.

**User-visible behavior or operational effect.**
The file contains information about a cluster, including its ID, version, bump, event count, start and end times, and a diff object with various metrics. The diff object provides insights into the structural, API, dependencies, runtime, and touched paths of the cluster.

**Maintainer notes and review checklist.**
Maintainers should:

- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.

The file's content is a JSON object with the following properties:

- `cluster_id`: The unique ID of the cluster.
- `version`: The version of the cluster.
- `bump`: The type of bump (Patch, Minor, Major).
- `event_count`: The number of events in the cluster.
- `started_at` and `ended_at`: The start and end times of the cluster.
- `diff`: An object with various metrics, including structural, API, dependencies, runtime, and touched paths.
- `weight`: An object with a score and API breaking/added flags.
- `air_gapped`: A boolean indicating whether the cluster is air-gapped.

The file's content is likely generated by a tool or process, and its purpose is to provide insights into the cluster's behavior and performance.
<!-- DUMDUM:END 15971414955467102700 -->

<!-- DUMDUM:START 16645294968037721344 -->
## `.kaptaind/status.json`

**Documentation depth:** brief explanation, target 260-380 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- Maintainer notes and review checklist.

**What it is:** This is a JSON file in `bart`. It configures tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters:** It configures tooling or runtime behavior rather than directly serving end-user screens. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Users may not touch this file directly, but its behavior can still affect reliability, output, or workflow.

**How it works:** The first meaningful line and surrounding directory are the strongest signals for this file. If that signal is weak, inspect imports, callers, or links before treating the explanation as complete.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 6 lines, 0 detected function-like definitions, hash 5444122364409979220.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 16645294968037721344 -->

<!-- DUMDUM:START 3871504057491408323 -->
## `.kaptaind/telemetry.json`

**Documentation depth:** brief explanation, target 260-380 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- Maintainer notes and review checklist.

**What it is:** This is a JSON file in `bart`. It configures tooling or runtime behavior rather than directly serving end-user screens.

**Why it matters:** It configures tooling or runtime behavior rather than directly serving end-user screens. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Users may not touch this file directly, but its behavior can still affect reliability, output, or workflow.

**How it works:** The first meaningful line and surrounding directory are the strongest signals for this file. If that signal is weak, inspect imports, callers, or links before treating the explanation as complete.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 12 lines, 0 detected function-like definitions, hash 7500609368165211234.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 3871504057491408323 -->

<!-- DUMDUM:START 17400636006978628185 -->
## `.kaptaind/version_cache.json`

**.kaptaind/version_cache.json**
==============================

**What it is and where it sits in the project.**
--------------------------------------------

This is a JSON file located in the `.kaptaind` directory of the `bart` project. It contains metadata about the project's dependencies and their versions.

**Why it matters to users or maintainers.**
-----------------------------------------

This file is used to cache information about the project's dependencies, specifically the Rust version detected from the `Cargo.toml` file. This information can be useful for maintainers to track changes in dependencies and ensure that the project is using the correct versions.

**User-visible behavior or operational effect.**
----------------------------------------------

Users may not directly interact with this file, but its contents can affect the project's behavior and output. For example, if the cached Rust version is outdated, it may cause issues with the project's compilation or execution.

**Maintainer notes and review checklist.**
-----------------------------------------

* Keep the generated explanation aligned when this file changes.
* Confirm that the explanation still matches the file after major edits.
* Check whether linked commands, images, GIFs, or VHS tapes still exist.
* Re-run DumDum after the file has rested so generated sections stay aligned.

**Media and demos.**
-------------------

No inline GIF, image, or VHS recording references were detected in this snapshot.

**Example Use Case.**
--------------------

This file can be used to track changes in dependencies and ensure that the project is using the correct versions. For example, if a new version of Rust is released, the maintainer can update the `Cargo.toml` file and re-run the project to cache the new version in this file.

```json
{
  "entries": {
    "rust": {
      "version": "2021",
      "detected_from": "Cargo.toml",
      "cached_at": 1783089469
    }
  }
}
```

In this example, the file contains metadata about the Rust version, including the version number, the file from which it was detected, and the timestamp when it was cached.
<!-- DUMDUM:END 17400636006978628185 -->

<!-- DUMDUM:START 8693988828248445818 -->
## `AGENTS.md`

**Documentation depth:** brief explanation, target 260-380 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- Maintainer notes and review checklist.

**What it is:** This is a Markdown file in `bart`. It provides prose documentation that explains how people should understand or operate the project.

**Why it matters:** It provides prose documentation that explains how people should understand or operate the project. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Readers may rely on this text as the source of truth, so stale wording can mislead them.

**How it works:** This is prose documentation. Its main behavior is social and operational: people will use it as a source of truth, so stale steps or screenshots are defects.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 46 lines, 0 detected function-like definitions, hash 16719414975012223411.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 8693988828248445818 -->

<!-- DUMDUM:START 939883841968491335 -->
## `Cargo.toml`

**What it is and where it sits in the project.**
The `Cargo.toml` file is a configuration file for the `bart` project, written in TOML (Tom's Obvious, Minimal Language). It is located at the root of the project directory.

**Why it matters to users or maintainers.**
This file is crucial for users and maintainers because it specifies the dependencies required by the `bart` project. It helps ensure that the project can be built and run correctly, and that the correct versions of dependencies are used.

**User-visible behavior or operational effect.**
The `Cargo.toml` file does not directly affect user-visible behavior or operational effect. However, it is used by the Rust build system to resolve dependencies and build the project. If the dependencies specified in this file are not correct, the project may not build or run correctly.

**Maintainer notes and review checklist.**

* Review the dependencies specified in this file to ensure they are up-to-date and correct.
* Verify that the project builds and runs correctly after making changes to this file.
* Consider using a tool like `cargo-tree` to visualize the dependency graph and ensure that there are no circular dependencies.
* Keep the `Cargo.toml` file organized and easy to read by using clear and concise dependency names and versions.
* Consider using a `Cargo.lock` file to pin the dependencies to specific versions and ensure reproducibility.
<!-- DUMDUM:END 939883841968491335 -->

<!-- DUMDUM:START 4545767476213472610 -->
## `README.md`

**Documentation depth:** standard explanation, target 520-760 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- How the important functions, settings, or document sections work together.
- Maintainer notes and review checklist.
- Inline media references and whether they should render in the library viewer.

**What it is:** This is a Markdown file in `bart`. It provides prose documentation that explains how people should understand or operate the project.

**Why it matters:** It provides prose documentation that explains how people should understand or operate the project. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Readers may rely on this text as the source of truth, so stale wording can mislead them.

**How it works:** This is prose documentation. Its main behavior is social and operational: people will use it as a source of truth, so stale steps or screenshots are defects.

**Media and demos:** Inline Markdown media references detected. DumDum's library viewer serves relative GIF/image links through `/api/media`.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 132 lines, 0 detected function-like definitions, hash 12244017823546280862.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 4545767476213472610 -->

<!-- DUMDUM:START 3746894761647837851 -->
## `assets/vhs/daemon_observability.tape`

**Documentation depth:** deep explanation, target 850-1200 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- How the important functions, settings, or document sections work together.
- Failure modes, security concerns, and testing guidance.
- Maintainer notes and review checklist.
- VHS recording intent, terminal steps, and expected GIF output.

**What it is:** This is a VHS file in `bart`. It is a VHS terminal recording script that documents a workflow by producing an animated capture.

**Why it matters:** It is a VHS terminal recording script that documents a workflow by producing an animated capture. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Users may see the generated GIF or terminal demo that comes from this script, so broken commands or stale timing make the documentation misleading.

**How it works:** This VHS tape is an executable recording script. It should be reviewed as both documentation and automation: the shell commands need to be deterministic, the viewport should fit the captured terminal, and the expected GIF should be regenerated when the demonstrated workflow changes.

**Media and demos:** VHS tape detected. Keep the `.tape` script near its generated `.gif` when possible.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 37 lines, 0 detected function-like definitions, hash 1590512526820133503.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 3746894761647837851 -->

<!-- DUMDUM:START 12973685599558714110 -->
## `assets/vhs/differential.tape`

**Documentation for assets/vhs/differential.tape**

**What it is and where it sits in the project**

The file `assets/vhs/differential.tape` is a VHS script located in the `assets/vhs` directory of the `bart` project. It is used to generate a GIF animation that demonstrates the differential scanning feature of the `bart` tool.

**Why it matters to users or maintainers**

This file is important because it provides a visual representation of how the differential scanning feature works. It can be used to educate users about the feature and help them understand its behavior. Maintainers can also use this file to test and verify the feature's functionality.

**User-visible behavior or operational effect**

When run, this script generates a GIF animation that shows the differential scanning feature in action. The animation demonstrates how the feature works by simulating a change in the file system and then showing the diff between the original and updated file system.

**How the important functions, settings, or document sections work together**

The script uses a combination of VHS commands to set up the environment, simulate a change, and then show the diff. The key functions and settings used in this script are:

* `Set Theme "Catppuccin Macchiato"`: Sets the theme for the animation.
* `Set Shell "bash"`: Sets the shell used for the simulation.
* `Set FontSize 18`: Sets the font size for the animation.
* `Set Width 1200` and `Set Height 400`: Sets the dimensions of the animation window.
* `Set Padding 40`: Sets the padding around the animation window.
* `Hide`, `Show`, `Type`, `Enter`, `Ctrl+L`, `Sleep`, and `rm`: These are VHS commands used to control the animation and simulate the file system changes.

**Failure modes, security concerns, and testing guidance**

* Failure modes: If the script fails to run or generates an incorrect animation, it may indicate a problem with the `bart` tool or the VHS engine.
* Security concerns: This script does not contain any sensitive information and does not pose any security risks.
* Testing guidance: To test this script, simply run it using the VHS engine. The resulting GIF animation should demonstrate the differential scanning feature in action.

**Maintainer notes and review checklist**

* Review the script regularly to ensure it remains accurate and up-to-date.
* Verify that the animation generated by the script is correct and matches the expected behavior of the differential scanning feature.
* Update the script as needed to reflect changes to the `bart` tool or the VHS engine.

**VHS recording intent, terminal steps, and expected GIF output**

The intent of this script is to generate a GIF animation that demonstrates the differential scanning feature of the `bart` tool. The terminal steps to run this script are:

1. Save the script to a file named `differential.tape` in the `assets/vhs` directory.
2. Run the script using the VHS engine: `vhs assets/vhs/differential.tape`
3. The resulting GIF animation should be saved to a file named `differential.gif` in the `assets/vhs` directory.

The expected GIF output is an animation that shows the differential scanning feature in action. The animation should demonstrate how the feature works by simulating a change in the file system and then showing the diff between the original and updated file system.

Here is an example of what the GIF animation might look like:

[![differential.gif](assets/vhs/differential.gif)](assets/vhs/differential.gif)

Note: The actual GIF animation may vary depending on the specific implementation of the `bart` tool and the VHS engine.
<!-- DUMDUM:END 12973685599558714110 -->

<!-- DUMDUM:START 16465460210853564004 -->
## `assets/vhs/interactive_tui.tape`

**What it is and where it sits in the project.**
The file `assets/vhs/interactive_tui.tape` is a VHS script located in the `assets/vhs` directory of the `bart` project. It is a script that controls the behavior of a terminal-based user interface (TUI) and is used to generate a GIF output.

**Why it matters to users or maintainers.**
This file is important because it defines the behavior of the TUI, which is used to interact with the `bart` tool. The script controls the layout, font, and behavior of the TUI, making it a crucial part of the user experience. Maintainers should be aware of this file to ensure that any changes to the TUI do not break the script.

**User-visible behavior or operational effect.**
The script controls the behavior of the TUI, including the layout, font, and behavior of the interface. It also defines the actions that the user can perform, such as navigating through the menu and selecting options. The script is designed to generate a GIF output that demonstrates the behavior of the TUI.

**How the important functions, settings, or document sections work together.**
The script uses a series of commands to control the behavior of the TUI. These commands include setting the theme, shell, font size, and width and height of the TUI. The script also uses keyboard shortcuts, such as `Ctrl+L` and `Enter`, to interact with the TUI. The script is designed to be executed in a specific order, with each command building on the previous one to create the desired behavior.

**Failure modes, security concerns, and testing guidance.**
The script is designed to be executed in a specific order, and any changes to the script should be tested thoroughly to ensure that they do not break the behavior of the TUI. Maintainers should be aware of the potential security concerns associated with executing a script that controls the behavior of a TUI. The script should be executed in a controlled environment, such as a test harness, to ensure that it does not cause any unintended behavior.

**Maintainer notes and review checklist.**
Maintainers should review the script regularly to ensure that it is up-to-date and accurate. They should also test the script thoroughly to ensure that it does not cause any unintended behavior. The following checklist should be used to review the script:

* Review the script regularly to ensure that it is up-to-date and accurate.
* Test the script thoroughly to ensure that it does not cause any unintended behavior.
* Ensure that the script is executed in a controlled environment, such as a test harness.
* Review the script for any potential security concerns.

**VHS recording intent, terminal steps, and expected GIF output.**
The script is designed to generate a GIF output that demonstrates the behavior of the TUI. The script is executed in a terminal, and the following steps are taken to generate the GIF output:

1. The script is executed in a terminal, and the TUI is displayed.
2. The script sets the theme, shell, font size, and width and height of the TUI.
3. The script uses keyboard shortcuts, such as `Ctrl+L` and `Enter`, to interact with the TUI.
4. The script navigates through the menu and selects options.
5. The script generates a GIF output that demonstrates the behavior of the TUI.

The expected GIF output is a demonstration of the behavior of the TUI, including the layout, font, and behavior of the interface. The GIF output should show the user navigating through the menu and selecting options, as well as the behavior of the TUI in response to keyboard shortcuts.

Here is a Markdown image of the expected GIF output:
![interactive_tui.gif](assets/vhs/interactive_tui.gif)

Note: The image is not actually rendered here, but it would be a GIF file that demonstrates the behavior of the TUI.
<!-- DUMDUM:END 16465460210853564004 -->

<!-- DUMDUM:START 17343352019823573688 -->
## `assets/vhs/scan_core.tape`

**Documentation for assets/vhs/scan_core.tape**

**What it is and where it sits in the project**

`assets/vhs/scan_core.tape` is a VHS script file located in the `assets/vhs` directory of the `bart` project. It is used to generate a GIF animation for the `scan_core` functionality.

**Why it matters to users or maintainers**

This file is important because it defines the behavior of the `scan_core` functionality, which is a key feature of the `bart` project. The animation generated by this script provides a visual representation of how the `scan_core` functionality works, making it easier for users to understand and interact with the project.

**User-visible behavior or operational effect**

When run, this script generates a GIF animation that shows the `scan_core` functionality in action. The animation includes a terminal recording of the commands being executed, along with visual effects such as typing, cursor movement, and screen updates.

**How the important functions, settings, or document sections work together**

The script uses a combination of VHS commands to set up the animation environment, define the theme, shell, font size, and other settings. It then uses the `Type` command to enter commands into the terminal, and the `Sleep` command to pause the animation at specific points. The `Hide` and `Show` commands are used to control the visibility of the terminal output.

**Failure modes, security concerns, and testing guidance**

* Failure modes: If the script is not properly formatted or contains errors, it may not generate the expected animation or may cause the VHS player to crash.
* Security concerns: As with any script that executes commands in a terminal, there is a risk of executing malicious code if the script is not properly sanitized.
* Testing guidance: To test this script, run it using a VHS player and verify that the animation is generated correctly. Test the script with different inputs and scenarios to ensure that it behaves as expected.

**Maintainer notes and review checklist**

* Review the script regularly to ensure that it remains up-to-date and accurate.
* Verify that the animation is generated correctly and that the script is properly formatted.
* Test the script with different inputs and scenarios to ensure that it behaves as expected.

**VHS recording intent, terminal steps, and expected GIF output**

The script is intended to generate a GIF animation that shows the `scan_core` functionality in action. The terminal steps are as follows:

1. Set the theme to "Catppuccin Macchiato"
2. Set the shell to "bash"
3. Set the font size to 18
4. Set the width to 1200
5. Set the height to 400
6. Set the padding to 40
7. Hide the terminal output
8. Type the command "export PATH=$PWD/target/release:$PATH"
9. Enter the command
10. Clear the terminal screen
11. Type the command "bart --depth 2"
12. Sleep for 500ms
13. Enter the command
14. Sleep for 4s

The expected GIF output is an animation that shows the terminal output and visual effects as the commands are executed.

Here is the expected GIF output:

[assets/vhs/scan_core.gif](assets/vhs/scan_core.gif)

Note: The GIF output is not included in this documentation, but it can be generated by running the script using a VHS player.
<!-- DUMDUM:END 17343352019823573688 -->

<!-- DUMDUM:START 13478515438333438456 -->
## `demo.tape`

**What it is and where it sits in the project.**
The `demo.tape` file is a VHS script located in the `assets/vhs` directory of the `bart` project. It is used to generate a GIF demonstration of the `bart` tool in action.

**Why it matters to users or maintainers.**
This file is important for users who want to see a visual representation of how the `bart` tool works. It also serves as a demonstration for maintainers to understand the tool's functionality and behavior.

**User-visible behavior or operational effect.**
When run, the `demo.tape` script generates a GIF demonstration of the `bart` tool performing various operations, such as scanning a directory, showing daemon status, enabling auto-indexing, and filtering mode. This GIF can be used to showcase the tool's capabilities to users.

**How the important functions, settings, or document sections work together.**
The `demo.tape` script uses a series of VHS commands to interact with the `bart` tool. These commands include setting the theme, shell, font size, and other display settings. The script then uses a sequence of `Type` commands to input commands to the `bart` tool, such as scanning a directory, showing daemon status, and enabling auto-indexing. The `Sleep` command is used to pause the script for a specified amount of time, allowing the user to see the output of each command.

**Failure modes, security concerns, and testing guidance.**
There are no obvious failure modes or security concerns associated with this script. However, it is possible that the script could fail if the `bart` tool is not installed or configured correctly. To test this script, simply run it using the `vhs` command, and it will generate a GIF demonstration of the `bart` tool in action.

**Maintainer notes and review checklist.**
Maintainers should review this script to ensure that it accurately demonstrates the functionality of the `bart` tool. They should also check that the script is up-to-date and reflects any changes to the tool's behavior. The review checklist should include:

* Confirm that the script still generates a valid GIF demonstration of the `bart` tool.
* Check that the script accurately reflects the current behavior of the `bart` tool.
* Verify that the script does not contain any sensitive or confidential information.

**VHS recording intent, terminal steps, and expected GIF output.**
The `demo.tape` script is intended to generate a GIF demonstration of the `bart` tool in action. The terminal steps to run this script are:

1. Run the `vhs` command with the `demo.tape` script as an argument: `vhs demo.tape`
2. The script will generate a GIF demonstration of the `bart` tool performing various operations.
3. The expected GIF output will show the `bart` tool scanning a directory, showing daemon status, enabling auto-indexing, and filtering mode.

Here is an example of what the GIF output might look like:

[Image: demo.gif](assets/vhs/demo.gif)

Note: This image is not actually generated by the script, but rather is a placeholder to illustrate what the output might look like.
<!-- DUMDUM:END 13478515438333438456 -->

<!-- DUMDUM:START 1291059866674548049 -->
## `kaptaind.toml`

**What it is and where it sits in the project.**
The file `kaptaind.toml` is a configuration file written in TOML (Tom's Obvious, Minimal Language) and is located in the root directory of the project.

**Why it matters to users or maintainers.**
This file configures the behavior of the `kaptaind` tool, which is used for monitoring the Vico ecosystem. The configuration options in this file affect how the tool collects and processes data, and how it interacts with the project's repository.

**User-visible behavior or operational effect.**
The configuration options in this file can affect the output and behavior of the `kaptaind` tool, such as the frequency of data collection, the types of data collected, and the thresholds for triggering alerts. Users may not directly interact with this file, but its behavior can still impact the reliability and output of the tool.

**Maintainer notes and review checklist.**
Maintainers should review this file to ensure that the configuration options are correct and up-to-date. They should also check that the linked commands, images, and VHS tapes still exist and are relevant to the project. After making changes to this file, maintainers should re-run DumDum to ensure that the generated sections stay aligned.

Here is a summary of the configuration options in this file:

* `watch`: Configures the file watching behavior of the `kaptaind` tool.
* `cluster`: Configures the clustering behavior of the `kaptaind` tool.
* `weights`: Configures the weights used by the `kaptaind` tool to calculate scores.
* `push`: Configures the push behavior of the `kaptaind` tool.
* `ratelimit`: Configures the rate limiting behavior of the `kaptaind` tool.
* `version_thresholds`: Configures the version thresholds used by the `kaptaind` tool.
* `test`: Configures the test behavior of the `kaptaind` tool.
* `staging`: Configures the staging behavior of the `kaptaind` tool.

Note that this file is not directly related to the VHS tapes or GIFs mentioned in the project context.
<!-- DUMDUM:END 1291059866674548049 -->

<!-- DUMDUM:START 9655211283461059977 -->
## `scripts/generate_demos.sh`

**Documentation depth:** brief explanation, target 260-380 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- Maintainer notes and review checklist.

**What it is:** This is a Shell file in `bart`. Its first useful signal is: set -e.

**Why it matters:** Its first useful signal is: set -e. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** Users may not touch this file directly, but its behavior can still affect reliability, output, or workflow.

**How it works:** The first meaningful line and surrounding directory are the strongest signals for this file. If that signal is weak, inspect imports, callers, or links before treating the explanation as complete.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 27 lines, 0 detected function-like definitions, hash 8775999676578760026.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 9655211283461059977 -->

<!-- DUMDUM:START 16113753218561945244 -->
## `src/main.rs`

**Documentation depth:** deep explanation, target 850-1200 words.

**Planned coverage:**
- What it is and where it sits in the project.
- Why it matters to users or maintainers.
- User-visible behavior or operational effect.
- How the important functions, settings, or document sections work together.
- Failure modes, security concerns, and testing guidance.
- Maintainer notes and review checklist.

**What it is:** This is a Rust file in `bart`. It is an executable entry point that wires command-line behavior to the rest of the program.

**Why it matters:** It is an executable entry point that wires command-line behavior to the rest of the program. DumDum treats this file as part of the project's working contract, so the explanation should connect the file to behavior, operations, or future maintenance rather than only restating its filename.

**What users should know:** The commands, flags, defaults, and error messages here shape the way users operate the tool.

**How it works:** The current snapshot has 1550 lines and 43 function-like definitions. Read the public functions first, then follow data flow into helpers before changing behavior.

**Media and demos:** No inline GIF, image, or VHS recording references were detected in this snapshot.

**Maintainer notes:** Keep the generated explanation aligned when this file changes. Current snapshot: 1550 lines, 43 detected function-like definitions, hash 17081104025884290883.

**Review checklist:**
- Confirm the explanation still matches the file after major edits.
- Check whether linked commands, images, GIFs, or VHS tapes still exist.
- Re-run DumDum after the file has rested so generated sections stay aligned.
<!-- DUMDUM:END 16113753218561945244 -->

